const a={slug:"latency",title:"Latency",emoji:"⚡",category:"Networking",tagline:"The time your system spends waiting, not working",roadmapKeywords:["latency","network","performance","throughput","cdn","cache"],related:["lld-symbols"],sections:[{heading:"What is Latency?",body:`Latency is the time that elapses between a cause and its effect — in systems, it is the delay between when a request is made and when the response begins to arrive. It is measured in milliseconds (ms), microseconds (µs), or even nanoseconds (ns) depending on the operation.

The key word is "begins to arrive." Latency is about delay, not volume. A fire hose has high throughput but can still have high latency if it takes 2 seconds before any water comes out.`,callout:{kind:"note",text:"Latency ≠ Bandwidth. Bandwidth is how much data can flow per second. Latency is how long the first byte takes to arrive. A satellite link can have high bandwidth but very high latency (~600 ms round trip)."}},{heading:"Real-World Analogy",body:`Think of ordering a pizza. You pick up the phone, dial, the restaurant answers, you place the order, they confirm, they cook it, and the delivery driver brings it to your door.

The moment you hang up after placing the order is when your "request" was sent. The moment the pizza arrives at your door is when the "response" is received. That entire duration — including the cooking time, packing, and driving — is the latency of your pizza order.

Meanwhile, the "bandwidth" of the delivery system is how many pizzas they can deliver per hour. The restaurant can have 10 drivers (high bandwidth) and still make you wait 45 minutes (high latency).`},{heading:"Request Journey — Where Latency Hides",body:"Every HTTP request you send travels through multiple layers, each adding latency. Understanding where the time goes is the first step to reducing it.",diagram:"latency-timeline",bullets:["DNS lookup: resolving the hostname to an IP address (can be 0 ms if cached, up to 100 ms if not)","TCP handshake: 3-way SYN → SYN-ACK → ACK exchange before any data flows","TLS handshake: 1–2 extra round trips to negotiate encryption (TLS 1.3 reduces this to 1-RTT)","Server processing: your actual business logic, DB queries, cache hits/misses","Data transfer: the time to transmit the response payload over the wire","Last-mile: the final hop from the ISP to the user's device — often the most unpredictable"]},{heading:"Latency Numbers Every Engineer Should Know",body:"These are rough order-of-magnitude numbers popularised by Peter Norvig and Jeff Dean. They are approximate but incredibly useful for back-of-the-envelope reasoning about system design.",diagram:"latency-table",callout:{kind:"tip",text:"The 10× rule of thumb: each layer up the stack adds roughly an order of magnitude in latency. L1 cache (1 ns) → L2 (10 ns) → RAM (100 ns) → SSD (100 µs) → HDD (10 ms) → Network (100 ms)."}},{heading:"Latency vs Throughput — The Classic Trade-off",body:`Latency and throughput are related but not the same thing, and optimising for one can hurt the other.

Batching is the classic example: sending 1,000 records as a single database INSERT is more throughput-efficient than 1,000 separate inserts, but the last record in the batch has to wait for all the others — higher latency for that individual record.

In system design interviews, always clarify which metric the problem cares about more. A real-time chat app needs low latency. A bulk data-export pipeline cares about throughput.`,table:{cols:["Metric","Definition","Optimised by"],rows:[["Latency","Time for one request to complete","Caching, CDN, connection pooling, fewer round trips"],["Throughput","Requests (or bytes) handled per second","Parallelism, batching, horizontal scaling"],["Bandwidth","Maximum data transfer capacity","Better network hardware, compression"],["P99 Latency","The 99th-percentile response time","Eliminating tail latencies, retry budgets"]]}},{heading:"P50, P95, P99 — Why Averages Lie",body:`An average latency of 50 ms can hide enormous pain. If 1% of your requests take 5 seconds, your users notice — even though the average looks fine.

Percentile latencies (p50, p95, p99, p999) tell a much better story:
- p50 (median): half of requests are faster than this
- p95: 95% of requests are faster than this — only 1 in 20 is slower
- p99: 99% of requests are faster — only 1 in 100 is slower
- p999: the "tail" — 1 in 1,000 requests that can be catastrophically slow

In distributed systems, tail latency compounds. If a request fans out to 100 microservices, and each has 1% chance of a 5s tail, the probability of hitting at least one tail is almost certain. This is why teams obsess over p99 and p999.`,callout:{kind:"warning",text:"Never report only average latency in a design review. Always show p50/p95/p99. Averages hide the worst-case experience your slowest users are having."}},{heading:"How to Reduce Latency",bullets:["Cache aggressively — serve from memory instead of disk or network (Redis, in-process LRU cache)","Move data closer to users — CDN edge nodes serve static assets from the nearest PoP","Reduce round trips — HTTP/2 multiplexing, GraphQL batching, persistent connections","Use faster storage — NVMe SSDs vs spinning HDDs, or in-memory DBs for hot paths","Avoid synchronous calls in serial — fan out parallel requests with Promise.all or async workers","TCP keep-alive + connection pooling — avoid paying the TCP handshake cost on every request","TLS 1.3 + QUIC — fewer handshake round trips; QUIC runs over UDP to avoid TCP head-of-line blocking","Pre-compute and pre-warm — do expensive work ahead of time (search index, recommendation model)"]}]},s={slug:"lld-symbols",title:"UML / LLD Diagram Symbols",emoji:"📐",category:"LLD",tagline:"What every arrow and box in a class diagram actually means",roadmapKeywords:["uml","lld","class diagram","low level design","design patterns","solid"],related:["latency"],sections:[{heading:"Why Do These Matter?",body:`Low-Level Design (LLD) interviews expect you to draw class diagrams that clearly communicate ownership, dependencies, and lifecycles between objects. Using the wrong symbol sends the wrong signal — a composition arrow when you mean aggregation changes the meaning entirely.

The good news: once you internalize 6 relationships, you can read and draw any class diagram correctly.`,callout:{kind:"note",text:"UML (Unified Modeling Language) is the standard. When your interviewer says 'draw a class diagram', they expect UML notation. These symbols are not company-specific — they are universal."}},{heading:"The Class Box — Building Block of Every Diagram",body:`Every entity in an LLD diagram is a class box. It is a rectangle divided into three compartments (though lower compartments can be omitted when not relevant):

1. Top section — Class name (bold, centred). If the class is abstract, the name is in italics.
2. Middle section — Attributes (fields), one per line. Format: visibility name: Type
3. Bottom section — Methods (operations), one per line. Format: visibility name(params): ReturnType

Visibility symbols:
+ means public   − means private   # means protected   ~ means package-level`,diagram:"lld-symbols",bullets:["Interface: same rectangle but with «interface» stereotype label above the name, or a small lollipop circle on the line","Abstract class: class name written in italics","Enum: «enumeration» stereotype label above the name"]},{heading:"The 6 Relationships You Must Know",body:"This is where most people get confused. There are 6 ways two classes can relate, and each has a distinct arrow. The key question to always ask: if Class A is destroyed, what happens to Class B?",table:{cols:["Relationship","Arrow Style","Strength","Destroyed together?","Example"],rows:[["Association","A ——→ B  (solid, open arrowhead)","Weakest","No","Student knows Teacher"],["Dependency","A - - → B  (dashed, open arrowhead)","Very weak","No","OrderService uses PaymentDTO"],["Aggregation","A ◇——→ B  (solid, hollow diamond at A)","Weak","No","Team has Players (players survive without team)"],["Composition","A ◆——→ B  (solid, filled diamond at A)","Strong","Yes","House has Rooms (rooms die with house)"],["Inheritance","A ——▷ B  (solid, hollow triangle at B)","N/A","N/A","Dog extends Animal"],["Realization","A - -▷ B  (dashed, hollow triangle at B)","N/A","N/A","Duck implements Flyable"]]}},{heading:"Association — 'Knows About'",body:`An association simply means one class holds a reference to another. It is the most general relationship — use it when an object uses another object but neither owns nor creates it.

The arrow points from the class that holds the reference to the class being referenced. You can add multiplicity labels (1, *, 0..1, 1..*) near the arrowheads to show cardinality.`,bullets:["Bidirectional association: no arrowheads, or arrowheads on both ends","Unidirectional: single arrowhead showing direction of navigation","Example: a Student is associated with many Courses. A Course is associated with many Students.","In code: the referencing class holds the other as a field or method parameter"],callout:{kind:"tip",text:"Association is the default relationship. If you are unsure which to use and the two classes simply know about each other, use association and annotate it with the cardinality."}},{heading:"Aggregation vs Composition — The Most Confused Pair",body:`Both aggregation and composition are 'has-a' relationships shown with a diamond at the owning end. The diamond is the key visual difference from a plain association arrow, and the fill tells you ownership strength.

Aggregation (◇ hollow diamond): the child can exist independently of the parent. If you delete the parent, the child survives. Think of a Team and its Players — if the team disbands, the players are still people.

Composition (◆ filled diamond): the child cannot exist without the parent. If you delete the parent, the child is destroyed too. Think of a House and its Rooms — if you demolish the house, the rooms cease to exist.

In code, composition usually means the parent is responsible for creating and destroying the child object (it is newed up inside the constructor, not passed in). Aggregation usually means the child is passed in from outside (dependency injection).`,callout:{kind:"warning",text:"The filled vs hollow diamond is the only visual difference between composition and aggregation. Get this wrong in an interview and you're implying a completely different lifecycle. When in doubt, ask yourself: 'Can the child survive without the parent?' If yes → aggregation (hollow). If no → composition (filled)."}},{heading:"Inheritance — 'Is-A'",body:`Inheritance (generalisation) is shown with a solid line and a hollow triangle (open arrowhead) pointing at the parent class. It represents the is-a relationship: a Dog is-a Animal.

The arrow always points from the child (subclass) to the parent (superclass). The hollow triangle sits at the parent end.

Realization is the interface equivalent: a class realizing (implementing) an interface. Same hollow triangle, but the line is dashed.`,bullets:["Solid line + hollow triangle → class extending another class (extends in Java/TypeScript)","Dashed line + hollow triangle → class implementing an interface (implements)","Multiple realization arrows are fine — a class can implement many interfaces","Multiple inheritance lines are rare in most OOP languages (Java/C# forbid it for classes)"]},{heading:"Dependency — 'Uses Temporarily'",body:`A dependency is the weakest, most transient relationship. Class A depends on Class B if A uses B but does not hold a long-lived reference — typically B appears only as a method parameter, a local variable, or a return type.

Drawn as a dashed line with an open arrowhead, pointing from the using class to the used class.`,bullets:["A service method that takes a DTO as a parameter and returns a response DTO — dependency","A class that calls a static utility method — dependency on that utility class","If B changes its interface, A may need to change — that is why it is called a dependency","Prefer to minimise dependencies; too many dashed arrows signal a tightly coupled design"]},{heading:"Quick Reference Cheat Sheet",body:"Use this table as a fast lookup when drawing or reading a diagram.",table:{cols:["Arrow","Name","Line","Head","Diamond","Meaning"],rows:[["——→","Association","Solid","Open arrow","—","A knows B / uses B long-term"],["- -→","Dependency","Dashed","Open arrow","—","A uses B temporarily (method param)"],["◇——→","Aggregation","Solid","Open arrow","Hollow at A","A has B; B can exist alone"],["◆——→","Composition","Solid","Open arrow","Filled at A","A owns B; B dies with A"],["——▷","Inheritance","Solid","Hollow triangle","—","A extends B (is-a)"],["- -▷","Realization","Dashed","Hollow triangle","—","A implements B (interface)"]]}}]},o={slug:"caching",title:"Caching",emoji:"🗄️",category:"Architecture",tagline:"The fastest request is one you never make",roadmapKeywords:["cache","caching","redis","memcached","cdn","eviction","lru","write-through"],related:["latency","cdn","database-indexes"],sections:[{heading:"What is a Cache?",body:`A cache is a fast, temporary storage layer that holds a copy of expensive-to-compute or expensive-to-fetch data so that future requests for the same data can be served faster.

The fundamental insight: most real-world workloads follow the Pareto principle (80/20 rule). About 20% of your data accounts for 80% of all reads. If you can keep that 20% in fast memory, you eliminate 80% of expensive trips to the database or downstream services.

Caches trade memory for speed. RAM is orders of magnitude faster than disk and network — an L1 cache hit takes ~1 ns; a database query over a network takes ~10 ms. That is 7 orders of magnitude difference.`,callout:{kind:"note",text:"A cache only helps if you read the same data more than once. If every request fetches unique data (like a unique report for each user), caching provides no benefit and adds complexity for nothing."}},{heading:"Cache Hit, Miss, and Eviction",body:`Every cache interaction results in one of three outcomes: a hit, a miss, or an eviction.

A cache hit occurs when the requested data is already in the cache. The system returns the cached copy immediately — no trip to the database. This is the happy path and the whole point of caching.

A cache miss occurs when the requested data is not in the cache. The system must go to the original data source (database, external API, etc.), retrieve the data, store it in the cache, and then return it. The first request for any new data is always a miss.

Eviction happens when the cache is full and new data needs to be stored. The cache must remove (evict) an existing entry to make room. Which entry gets evicted depends on the eviction policy.

Cache hit rate = hits / (hits + misses). A well-tuned cache for a read-heavy workload should achieve 90%+ hit rate.`,diagram:"cache-hit-miss"},{heading:"Eviction Policies",body:"When the cache is full, the eviction policy determines which entry to remove. Choosing the right policy for your access pattern is critical.",table:{cols:["Policy","Full Name","How it works","Best for","Weakness"],rows:[["LRU","Least Recently Used","Evicts the entry that hasn't been accessed for the longest time","General purpose — most workloads","Doesn't account for access frequency; a one-time scan evicts hot entries"],["LFU","Least Frequently Used","Evicts the entry with the fewest total accesses","Workloads where popular items stay popular","New items start with count=1 and are immediately eviction candidates (startup problem)"],["FIFO","First In First Out","Evicts the oldest-added entry regardless of access pattern","Simplicity, rotating content (news feeds, logs)","Ignores recency and frequency entirely — poor hit rate in practice"],["TTL","Time To Live","Each entry expires after a fixed duration and is evicted","Data with natural staleness (sessions, tokens, prices)","Setting TTL too short = cache misses; too long = stale data"],["Random","Random Replacement","Evicts a random entry","Approximate counting, simple caches","Unpredictable; rarely used in production"],["ARC","Adaptive Replacement Cache","Dynamically balances between LRU and LFU lists","Mixed/unpredictable access patterns","More complex to implement; used in ZFS, PostgreSQL"]]},callout:{kind:"tip",text:"Redis uses LRU (with configurable approximation). Memcached uses LRU. Most application caches default to LRU. Start with LRU — only switch if profiling shows your access pattern is better served by LFU."}},{heading:"Write Strategies",body:"When data is written (INSERT/UPDATE), the cache and the database can get out of sync. Write strategy determines how and when the cache is updated.",table:{cols:["Strategy","How it works","Consistency","Write speed","Risk"],rows:[["Write-through","Write to cache and DB simultaneously. Both always in sync.","Strong","Slow (blocked on DB)","Every write pays full DB cost — no write throughput benefit"],["Write-back (write-behind)","Write to cache only. Asynchronously flush to DB later.","Eventual","Fast (in-memory)","Data loss if cache crashes before flush; complex retry logic"],["Write-around","Write directly to DB, skip cache. Cache is populated on next read miss.","Strong","Normal DB speed","Cold cache on write-heavy bursts; first read after write always misses"],["Read-through","On miss, cache fetches from DB, stores it, returns it. App only talks to cache.","Strong (on read)","Normal","Cold start has miss penalty; cache and DB must stay in sync on writes"],["Cache-aside (Lazy loading)","App checks cache. On miss, app fetches from DB, populates cache itself.","Eventual","Normal","Race condition: two threads both miss, both fetch, one overwrites the other"]]},callout:{kind:"warning",text:"Cache-aside with write-invalidation is the most common pattern in practice. On write: delete the cache key (don't update it). On next read: miss → fetch from DB → repopulate. This avoids the race condition where a slow write puts stale data into the cache."}},{heading:"Where to Cache — The Caching Hierarchy",body:"Caching can be applied at multiple layers. Each layer has different trade-offs in speed, capacity, and scope.",bullets:["Client-side (browser cache): HTTP Cache-Control headers instruct the browser to cache responses. Zero network cost on a cache hit. Limited to per-user data and public assets. Great for static files (JS, CSS, images).","CDN cache (edge cache): Geographically distributed caches that serve static and semi-static content from the PoP closest to the user. Eliminates round trips to your origin server. Controlled via Cache-Control and Surrogate-Control headers.","Reverse proxy cache (Nginx, Varnish): Sits in front of your application servers. Caches full HTTP responses. Excellent for public pages that are expensive to render but identical for all users.","Application cache (Redis, Memcached): In-memory cache your application manages directly. Full control over keys, TTLs, and invalidation. Best for: computed results, session data, rate-limit counters, leaderboards, and any data that doesn't map to a full HTTP response.","Database query cache: Some databases (MySQL historically, PostgreSQL via pg_buffercache) cache query results or buffer pool pages internally. Generally not relied upon — application-level caching gives you more control.","CPU / hardware cache (L1/L2/L3): Managed by hardware, not your code. Relevant for high-performance computing but invisible to most application-level engineers."]},{heading:"Cache Stampede (Thundering Herd)",body:`Cache stampede is a failure mode where a popular cache entry expires, and hundreds or thousands of concurrent requests all experience a miss simultaneously. Every request races to the database to repopulate the cache. The database gets hammered with the same expensive query N times — exactly what the cache was supposed to prevent.

This is most dangerous for high-traffic keys with expensive backend computation (e.g., the front page of a popular website, a trending leaderboard, a report that takes 2 seconds to generate).

Solutions:
1. Mutex/lock on miss: Only one request fetches from the DB; others wait and read from cache once populated. Introduces latency for waiting requests.
2. Probabilistic early expiration (PER/XFetch): Before TTL expires, randomly some requests treat the key as expired and refresh it early. Amortises the refresh over time with no coordination.
3. Background refresh: A background job refreshes cache entries before they expire. The cache never actually goes empty for hot keys. Best for keys with predictable TTLs.
4. Stale-while-revalidate: Serve the stale cached value immediately while triggering an async refresh in the background. User sees slightly old data but never a slow response.`,callout:{kind:"tip",text:"The simplest production fix: add a small random jitter to TTLs (e.g., TTL = 300 + random(0, 60) seconds). This spreads expiration events across time so thousands of keys don't all expire at the exact same moment."}},{heading:"Redis vs Memcached",table:{cols:["Feature","Redis","Memcached"],rows:[["Data structures","Strings, Hashes, Lists, Sets, Sorted Sets, Streams, HyperLogLog, Bitmaps, Geo","Strings only"],["Persistence","RDB snapshots + AOF append-only log (optional)","None — in-memory only"],["Replication","Primary-replica, Redis Sentinel, Redis Cluster","No built-in replication"],["Pub/Sub","Yes — built-in channel-based messaging","No"],["Lua scripting","Yes — atomic server-side scripts","No"],["Multi-threading","Single-threaded command execution (I/O is async multi-thread since Redis 6)","Multi-threaded from the start"],["Max value size","512 MB per key","1 MB per key"],["Use cases","Everything — sessions, queues, leaderboards, rate limiting, pub/sub, geospatial","Simple string KV cache with extreme read throughput"],["When to choose","Default choice for almost all new projects","Legacy systems or when you need raw multi-core KV throughput"]]}}]},i={slug:"cap-theorem",title:"CAP Theorem",emoji:"🔺",category:"Distributed Systems",tagline:"Pick two: Consistency, Availability, Partition Tolerance",roadmapKeywords:["cap","consistency","availability","partition","distributed","base","pacelc"],related:["consistent-hashing","replication","acid-transactions"],sections:[{heading:"The Problem CAP Theorem Addresses",body:`The moment you split your database across more than one machine, you have a distributed system. Distributed systems communicate over a network, and networks are fundamentally unreliable — packets get lost, links go down, switches fail, and data centres get isolated from each other.

Eric Brewer formalised this in 2000 (proven by Gilbert and Lynch in 2002): a distributed data store can guarantee at most two of the three following properties at any given time.`},{heading:"The Three Properties",body:`Consistency (C): Every read receives the most recent write or an error. All nodes in the cluster see the same data at the same time. When you write to any node, every subsequent read from any node returns that value. This is linearizability — the strongest consistency model.

Availability (A): Every request receives a (non-error) response — but it might not be the most recent data. Every node that is up will respond, even if that response is stale. A system is available if it keeps answering requests even when some nodes are down.

Partition Tolerance (P): The system continues to operate even when network messages between nodes are lost or delayed arbitrarily. A partition is a network split — some nodes can't talk to others.`,diagram:"cap-triangle",callout:{kind:"warning",text:"Partition Tolerance is NOT optional in any real distributed system. Networks partition. It's a matter of when, not if. So in practice, the choice is always between CP (sacrifice availability during a partition) and AP (sacrifice consistency during a partition). CA systems can only exist on a single machine."}},{heading:"What Happens During a Partition",body:`When a network partition occurs — Node A can't talk to Node B — a distributed system must make a choice:

Option 1 — Maintain Consistency (CP): Refuse to answer requests on the minority side of the partition. Some nodes return errors or timeouts until the partition heals. The system is correct but unavailable on those nodes.

Option 2 — Maintain Availability (AP): Keep answering requests on both sides of the partition, even though nodes might return stale or conflicting data. The system stays up but might be incorrect.

There is no third option. If you require both nodes to answer AND to always return fresh data during a split — you've violated one of the two properties. Math doesn't negotiate.`},{heading:"CP Systems — Consistent but can become Unavailable",table:{cols:["System","Type","How it achieves CP","Trade-off"],rows:[["Apache ZooKeeper","Coordination service","Paxos-based consensus; writes go through a single leader; minority partition refuses writes","Writes are slow; ZooKeeper ensemble becomes unavailable if leader is partitioned"],["HBase","Wide-column DB","Strongly consistent reads/writes via HDFS; a region server outage means that region is unavailable","Lower availability; operations can fail during region server failures"],["Google Spanner","NewSQL / Global DB","TrueTime API + Paxos consensus across data centres","High latency for globally synchronous writes"],["etcd","Key-value store","Raft consensus; leader election; minority partition refuses writes","Same as ZooKeeper — leader loss causes brief unavailability"],["MongoDB (w:majority)","Document DB","Majority writes ensure durability before ack; reads from primary","If primary is unreachable, replica set refuses writes until election completes"]]}},{heading:"AP Systems — Available but Eventually Consistent",table:{cols:["System","Type","How it achieves AP","Consistency model"],rows:[["Apache Cassandra","Wide-column DB","Leaderless; any node can accept writes; gossip protocol syncs nodes eventually","Eventual consistency; tunable (QUORUM, ONE, ALL)"],["Amazon DynamoDB","Key-value / Doc DB","Multi-region active-active replication; always accepts writes","Eventual (default) or strong (strongly consistent reads, extra cost)"],["CouchDB","Document DB","Multi-master; conflicts resolved via MVCC and revision tracking","Eventual; conflict resolution required"],["DNS","Name resolution","Responses are cached at multiple levels; updates propagate via TTL expiry","Eventual — TTL propagation can take minutes to hours"],["Riak","Key-value store","Dynamo-style leaderless with vector clocks for conflict detection","Eventual; last-write-wins or application-level resolution"]]}},{heading:"PACELC — Beyond CAP",body:`CAP only describes what happens during a partition. But partitions are rare — most of the time your network is healthy. During normal operation, there's still a trade-off between latency and consistency.

Daniel Abadi proposed PACELC (2012):
- If there is a Partition (P): choose between Availability (A) and Consistency (C)  
- Else (E): choose between Latency (L) and Consistency (C)

Example: DynamoDB is PA/EL — it picks availability over consistency during a partition, and picks low latency over strong consistency during normal operation.

Example: Spanner is PC/EC — it picks consistency always. During a partition and during normal operation, it enforces strong consistency, paying the latency cost.`,table:{cols:["System","PACELC classification","Meaning"],rows:[["DynamoDB","PA/EL","During partition: available. Normal: optimises for low latency (eventual consistency by default)"],["Cassandra","PA/EL","During partition: available. Normal: low latency tunable consistency"],["MongoDB","PC/EC","During partition: consistent (minority refuses writes). Normal: consistent reads from primary"],["Spanner","PC/EC","Always consistent, pays latency price"],["MySQL/Postgres (single-node)","PC/EC","Single node — no partition. Consistent reads, latency depends on hardware"]]},callout:{kind:"tip",text:"In an interview, don't just say 'AP or CP'. Say: 'This system is AP because during a partition we need to keep serving reads (e.g., product catalogue) even if some data is slightly stale. We accept eventual consistency and reconcile via last-write-wins.' Showing the trade-off reasoning is what interviewers are looking for."}},{heading:"Tunable Consistency — Having it Both Ways (sometimes)",body:`Modern systems like Cassandra and DynamoDB don't force you to pick one extreme. They let you tune consistency per request using quorum reads and writes.

For a cluster of N replicas, a write with W acknowledgements required and a read from R replicas is strongly consistent if R + W > N.

Example: N=3 replicas. Set W=2 (write must be acked by 2 nodes) and R=2 (read from 2 nodes). Since 2+2=4 > 3, at least one of your read nodes always has the latest write. Strong consistency — but you've sacrificed some availability (if 2 nodes are down, both W and R can't be satisfied).

Set W=1, R=1: maximum availability, eventual consistency.
Set W=3, R=1: strong write durability, fast reads — but any node outage blocks writes.`}]},r={slug:"load-balancing",title:"Load Balancing",emoji:"⚖️",category:"Architecture",tagline:"Spreading work so no single server drowns",roadmapKeywords:["load balancer","load balancing","nginx","round robin","sticky session","haproxy","l4","l7"],related:["caching","microservices","consistent-hashing"],sections:[{heading:"Why Load Balancers Exist",body:`A single server can only handle so many requests before it becomes the bottleneck. When traffic spikes — a product goes viral, a cron job triggers, a major news event drives traffic — you need to scale horizontally by adding more servers. But adding servers alone isn't enough: you need something to distribute requests across them.

A load balancer (LB) is a reverse proxy that sits between clients and your server pool. It accepts incoming connections and routes each request to one of the backend servers according to a distribution algorithm. The client doesn't know — or care — which backend handled its request.

Beyond traffic distribution, load balancers provide: health checking (removing failed servers automatically), SSL/TLS termination (decrypting HTTPS so backend servers don't need to), connection draining (gracefully removing servers during deployments), and request logging / metrics aggregation.`,diagram:"lb-flow"},{heading:"L4 vs L7 Load Balancing",body:"Load balancers operate at different layers of the OSI model. The layer determines what information the LB can use to make routing decisions.",table:{cols:["Property","L4 (Transport Layer)","L7 (Application Layer)"],rows:[["What it sees","IP address, TCP/UDP port, packet headers","HTTP headers, URL path, cookies, request body, TLS SNI"],["Routing basis","IP + port only","Any HTTP attribute: path, host, cookie, header"],["SSL termination","Pass-through only (no decryption)","Full SSL termination — decrypts, inspects, re-encrypts"],["Performance","Faster — minimal parsing","Slower — must parse HTTP fully"],["Content routing","Cannot route /api vs /static differently","Can route /api → API servers, /static → CDN or asset servers"],["Protocol support","Any TCP/UDP protocol","HTTP/1.1, HTTP/2, HTTP/3, WebSocket, gRPC"],["Examples","AWS NLB, HAProxy TCP mode","AWS ALB, Nginx, HAProxy HTTP mode, Envoy, Traefik"],["Use when","Low latency needed; non-HTTP protocols; raw throughput","HTTP microservices; content-based routing; SSL termination"]]}},{heading:"Load Balancing Algorithms",body:"The algorithm determines which backend server receives each incoming request. Different workloads benefit from different strategies.",table:{cols:["Algorithm","How it works","Best for","Weakness"],rows:[["Round Robin","Requests distributed in sequence: server 1, 2, 3, 1, 2, 3…","Homogeneous servers, uniform request cost","Ignores server load — a slow request keeps the server busy while more arrive"],["Weighted Round Robin","Same as round robin but servers with higher weight receive more requests proportionally","Heterogeneous servers (different CPU/RAM)","Still ignores real-time load — weights are static"],["Least Connections","Send to server with fewest active connections currently","Long-lived connections, mixed request durations","Doesn't account for connection weight (a heavy query ≠ a lightweight query)"],["Least Response Time","Send to server with lowest combination of active connections and response latency","Latency-sensitive applications","Requires real-time latency tracking; more complex"],["IP Hash","Hash client IP → always route to same server","Session affinity without cookies, stateful protocols","Hot spots if traffic from one IP subnet dominates; breaks when server pool changes"],["Random","Pick a server at random","Simplicity; works well at scale (law of large numbers)","Poor for small server counts or skewed request costs"],["Resource-based (Adaptive)","Route to server with most available CPU/RAM, reported via health check API","CPU/memory-intensive workloads, video encoding, ML inference","Requires servers to expose resource metrics; more complex"]]},callout:{kind:"tip",text:"Least Connections is the safe default for most HTTP APIs. Round Robin works fine when your requests have uniform cost and your servers are identical. Use IP Hash only when you need session stickiness and can't use cookies."}},{heading:"Health Checks",body:`A load balancer continuously probes backend servers to detect failures. When a server fails health checks, the LB stops routing traffic to it and redistributes to healthy servers.

Active health checks: the LB periodically sends its own probe requests to each backend (e.g., HTTP GET /health every 5 seconds). If the server responds with 2xx, it's healthy. If it times out or returns 5xx, the LB marks it unhealthy and removes it from rotation.

Passive health checks: the LB observes real traffic. If a server returns errors (5xx) or times out above a threshold, it's removed. Less aggressive than active checks but doesn't require a dedicated /health endpoint.

Health check configuration knobs: interval (how often to probe), timeout (how long to wait), unhealthy threshold (how many consecutive failures before removal), healthy threshold (how many successes before re-adding).`,callout:{kind:"note",text:"/health should be a lightweight endpoint that checks only what's needed to serve traffic: DB connection alive, critical caches warm. Don't check external dependencies — if a 3rd-party API is down, your server can still serve many routes just fine."}},{heading:"Sticky Sessions (Session Affinity)",body:`By default, the load balancer can send consecutive requests from the same user to different servers. For stateless APIs this is fine — but stateful applications that store session data in server memory break if requests are spread across servers.

Sticky sessions solve this by binding a user to a specific server for the duration of their session, usually via a cookie or IP hash.

The problem with sticky sessions: they reintroduce the uneven load problem. If one server's users are unusually active, that server gets more load than others. When a sticky server goes down, all its sessions are lost anyway. And they make rolling deployments harder — draining a server means waiting for all sticky sessions to expire.

The correct long-term fix: make your servers stateless. Move session storage into a shared store (Redis, database) that all servers can access. Then any server can handle any request. This is the right architecture.`,callout:{kind:"warning",text:"Sticky sessions are a band-aid. They work, but they defeat the horizontal scaling benefits of a load balancer. Architectural goal: stateless servers + shared session store."}},{heading:"Global Load Balancing (DNS-Based)",body:`Beyond distributing traffic across servers in one data centre, global load balancing distributes traffic across multiple data centres or cloud regions.

DNS-based global LB: return different IP addresses for the same domain based on the requester's geographic location. A user in Tokyo gets routed to the Tokyo data centre; a user in London gets the Frankfurt IP. Tools: AWS Route 53 (latency/geolocation routing), Cloudflare, Google Cloud DNS.

Anycast: a single IP address is announced from multiple locations simultaneously. The internet routes traffic to the nearest node automatically via BGP. Used by Cloudflare, Google's 8.8.8.8 DNS, CDNs.

Trade-offs: DNS-based LB relies on TTL for failover — during a data centre outage, users with the old IP cached keep hitting the dead DC until TTL expires. Using a very low TTL (30s) mitigates this but increases DNS query volume.`}]},n={slug:"database-indexes",title:"Database Indexes",emoji:"🔍",category:"Database",tagline:"The lookup table inside your database",roadmapKeywords:["index","indexes","b-tree","database","query","sql","postgres","covering index","composite index"],related:["sql-vs-nosql","acid-transactions","sharding"],sections:[{heading:"What is a Database Index?",body:`An index is a separate data structure maintained by the database that maps key values to the physical location of the corresponding rows on disk. It is analogous to the index at the back of a textbook: instead of reading every page to find "load balancing", you look it up in the index and jump directly to page 142.

Without an index, a query like SELECT * FROM orders WHERE user_id = 12345 requires a full table scan — the database reads every single row in the table and checks if user_id matches 12345. For a table with 100 million rows, this could take many seconds.

With an index on user_id, the database uses the index's data structure to find the matching rows in O(log n) time, then fetches only those rows. The same query might complete in milliseconds.

The trade-off: indexes are not free. Every index takes storage space. Every INSERT, UPDATE, or DELETE must also update all indexes on the affected columns — writes become slower as you add more indexes. Indexes are a read optimisation that costs write performance and storage.`,diagram:"btree-index"},{heading:"B-Tree Indexes — The Default Index Type",body:`Almost every relational database (PostgreSQL, MySQL, Oracle, SQL Server) uses a B-tree (Balanced Tree) as the default index structure. Understanding the B-tree helps you reason about which queries benefit from an index and which don't.

A B-tree is a self-balancing tree where every node can have multiple keys and multiple children. All leaf nodes are at the same depth (balanced). The tree is kept sorted, which enables efficient range queries.

B-tree properties that matter for SQL:
- Equality lookups (=): O(log n) — traverse from root to the leaf containing the key
- Range queries (BETWEEN, >, <, >=, <=): very efficient — find the starting leaf, then follow the linked-list pointers between leaves
- ORDER BY: "free" — data in the index is already sorted
- Prefix LIKE queries (LIKE 'John%'): works — the tree is sorted, prefix scan is efficient
- Suffix LIKE queries (LIKE '%son'): does NOT work — the tree doesn't sort by suffix

The database's query planner decides whether to use an index. If the planner estimates that a table scan is faster than using the index (e.g., for very low-cardinality columns, or when fetching > ~10% of rows), it will scan instead.`},{heading:"B-Tree vs Hash Index",table:{cols:["Property","B-Tree Index","Hash Index"],rows:[["Lookup","O(log n)","O(1) average"],["Range queries","Excellent","Not supported — hashes are not ordered"],["ORDER BY","Supported (already sorted)","Not supported"],["LIKE prefix","Supported","Not supported"],["Equality (=)","O(log n)","O(1) — faster for pure equality"],["Storage","Larger (stores sorted keys + pointers)","Smaller (just hash → rowid)"],["Default in","PostgreSQL, MySQL/InnoDB, Oracle, SQLite","PostgreSQL (explicit), MySQL Memory engine"],["Use when","Almost everything — ranges, sorting, prefix searches","Pure equality lookups where range/order never needed"]]},callout:{kind:"note",text:"PostgreSQL also offers GIN indexes (for full-text search, JSONB, arrays), GiST indexes (geometric data, full-text), and BRIN indexes (time-series tables where values correlate with physical row order). These are specialised — use B-tree until you have a specific reason to switch."}},{heading:"Composite Indexes — Column Order Matters Enormously",body:`A composite index is an index on multiple columns: CREATE INDEX idx_orders_user_status ON orders(user_id, status).

The leftmost prefix rule: a composite index on (A, B, C) can be used for queries that filter on: A alone, A+B together, or A+B+C together. It cannot be used efficiently for queries on B alone, C alone, or B+C without A.

This is because the B-tree sorts entries first by A, then by B within each A, then by C within each (A,B) group. If you query only on B, the data isn't sorted by B at the top level — you'd need a full index scan.

Rule of thumb: put the column with the highest cardinality first (the one with the most distinct values), unless you have a specific equality filter that should come first.

Example — index on (user_id, created_at):
- WHERE user_id = 5 → uses index ✓
- WHERE user_id = 5 AND created_at > '2025-01-01' → uses index ✓
- WHERE created_at > '2025-01-01' → CANNOT use index efficiently ✗ (need separate index on created_at)`},{heading:"Covering Indexes",body:`A covering index is an index that contains all the columns a query needs, so the database never has to read the actual table rows — all the data is in the index itself. This eliminates the most expensive step: the random I/O to fetch heap rows.

Example: SELECT user_id, status, total FROM orders WHERE user_id = 5

If the index is (user_id, status, total), the query can be satisfied entirely from the index. No heap access needed. This is called an index-only scan in PostgreSQL.

In PostgreSQL, you can use INCLUDE to add non-search columns to the index: CREATE INDEX idx_orders_covering ON orders(user_id) INCLUDE (status, total). This keeps the index key small (fast searches) while including extra columns for covering.`,callout:{kind:"tip",text:"Covering indexes are one of the biggest 'free' performance wins. Before adding a new index, check if you can extend an existing one with INCLUDE to cover frequently selected columns."}},{heading:"Partial and Expression Indexes",body:`Partial index: an index that only indexes rows matching a condition. CREATE INDEX idx_active_users ON users(email) WHERE deleted_at IS NULL. If 95% of your users are soft-deleted, this index is 20x smaller than a full index and stays more cache-friendly.

Expression index (functional index): index on an expression, not a column. CREATE INDEX idx_lower_email ON users(LOWER(email)). Enables case-insensitive lookups without a table scan. The query WHERE LOWER(email) = 'john@example.com' will use this index.`},{heading:"When NOT to Index",body:"More indexes is not always better. Indexes have real costs — evaluate carefully before adding one.",bullets:["Low-cardinality columns: a boolean column (is_active) has only 2 values. If 60% of rows are active, an index scan on is_active=true still reads 60% of the table — the planner will choose a full scan instead. Indexing this column wastes space and write overhead.","Small tables: if a table has fewer than ~1,000 rows, a full table scan fits in a few memory pages and is faster than the overhead of index lookup. The planner typically ignores indexes on small tables anyway.","Columns that are never queried: obvious, but easy to forget when copy-pasting index creation statements from tutorials.","Write-heavy tables: each index adds overhead to every INSERT/UPDATE/DELETE. A table with 10 indexes and 10,000 inserts/second is spending significant CPU on index maintenance. Profile first.","Columns that are always fetched with a full scan anyway: very wide range queries that return >20% of rows — the sequential scan is often faster because it's I/O-sequential vs random heap fetches."],callout:{kind:"warning",text:"Run EXPLAIN ANALYZE (PostgreSQL) or EXPLAIN (MySQL) on slow queries before adding an index. The query planner will show you whether an index is being used, which index, and what the actual cost is. Index blindly and you'll slow down your writes without helping your reads."}},{heading:"Index Cardinality and Selectivity",body:`Selectivity = (number of distinct values) / (total rows). A highly selective index (selectivity close to 1.0) is useful — almost every value maps to a small set of rows, so the index lookup is efficient.

A low-selectivity index (selectivity near 0) is useless for filtering. Indexing a gender column with 2 values returns half the table — the query planner will table-scan instead.

ANALYZE (PostgreSQL) / ANALYZE TABLE (MySQL) collects statistics about column cardinality. The query planner uses these statistics to estimate whether using an index is worth it. Run ANALYZE regularly or let autovacuum handle it.`}]},l={slug:"consistent-hashing",title:"Consistent Hashing",emoji:"🔄",category:"Distributed Systems",tagline:"Balanced by design — even when nodes come and go",roadmapKeywords:["consistent hashing","hash ring","sharding","partition","vnodes","cassandra","dynamo"],related:["sharding","cap-theorem","caching"],sections:[{heading:"The Problem with Naive Modulo Hashing",body:`The simplest way to distribute data across N servers is modulo hashing: server = hash(key) % N.

If you have 3 servers and hash("user:123") = 15, that key lives on server 15 % 3 = 0.

This works perfectly until N changes. When you add a 4th server, N becomes 4. Now hash("user:123") = 15 → 15 % 4 = 3. The key moved to server 3. In fact, almost every key changes its server assignment when N changes.

In a cache cluster with 3 nodes and 1 million cached keys: if you add or remove one node, approximately (N-1)/N = 67% of all keys become invalid cache misses. Every user suddenly fetches from the database. Your database collapses under the load.

In a distributed database: reshuffling 67% of data when you add one node means massive data transfer across the network. This could take hours and saturate your network.

Consistent hashing reduces this to O(K/N) key remappings when a node is added or removed — where K is the total number of keys and N is the number of nodes. Only the keys that were on the specific node being removed need to be redistributed.`},{heading:"The Hash Ring",body:`Consistent hashing maps both servers and data keys onto a conceptual ring (a number line that wraps around, with values 0 to 2^32 or 0 to 2^64 depending on the hash function).

Step 1 — Place servers on the ring: hash each server's identifier (hostname, IP, ID) to get a position on the ring. With 3 servers, you get 3 points distributed across the ring.

Step 2 — Place keys on the ring: hash each data key to get its position on the ring.

Step 3 — Route requests: walk clockwise from the key's position until you hit the first server. That's the server responsible for this key. This rule is fixed and deterministic.

When a server is added: it takes a position on the ring. Only the keys between the new server and its predecessor (anticlockwise) are remapped from the next server clockwise to the new server. All other keys are unaffected.

When a server is removed: only the keys it held are remapped to the next server clockwise. All other keys are unaffected.`,diagram:"hash-ring"},{heading:"Virtual Nodes (Vnodes) — Solving Hot Spots",body:`The basic ring has a problem: with few servers, the ring segments are uneven. If three servers hash to positions 10, 12, and 250 on a 0-255 ring, server at position 250 owns positions 12-250 — 93% of the ring! This server gets nearly all the traffic and data. The other two servers are idle.

Solution: virtual nodes (vnodes). Instead of placing each physical server once on the ring, place it at many positions. A server with 150 vnodes appears at 150 different ring positions, randomly distributed. Each vnode is a virtual replica of the physical server.

Benefits of vnodes:
1. Even data distribution: 150 random positions per server distributes load far more evenly than 1 position.
2. Graceful scaling: when adding a new server, it claims a few ring segments from each existing server rather than one large segment from one server.
3. Heterogeneous hardware: give more vnodes to more powerful servers. A server with twice the RAM can have twice as many vnodes and will hold twice as much data proportionally.

Cassandra defaults to 256 vnodes per physical server. DynamoDB uses a more complex variant internally.`,callout:{kind:"note",text:"Vnodes introduce overhead: the routing table grows (150 vnodes × N servers entries), and repair/rebalancing is more complex. The trade-off is worth it at scale, but for small clusters (< 10 nodes) the simple ring without vnodes is easier to reason about."}},{heading:"Replication with Consistent Hashing",body:`Consistent hashing makes replication straightforward. Instead of routing to just the first clockwise server, route to the first R clockwise servers. Each key has R replicas on R consecutive servers around the ring.

This is exactly what Cassandra's replication strategy does with its SimpleStrategy (single data centre). With replication factor 3, each row is stored on 3 consecutive servers in the ring.

Reads and writes use quorum mathematics (R + W > N) to determine consistency level — this ties directly to the CAP theorem trade-offs: choose more replicas to read from for stronger consistency, at the cost of availability during partial failures.`},{heading:"Real-World Usage",table:{cols:["System","How consistent hashing is used"],rows:[["Apache Cassandra","Primary partitioning strategy. Murmur3 hash of partition key → ring position. 256 vnodes per node by default. NetworkTopologyStrategy distributes replicas across racks."],["Amazon DynamoDB","Internal consistent hashing (details not public). Keys partitioned across storage nodes with automatic rebalancing. Users set partition key; DynamoDB handles the ring."],["Memcached clients","ketama algorithm — a consistent hash client library. Cache clients hash keys to cache servers. Adding a server only remaps ~1/N keys."],["CDN cache routing","Edge PoPs use consistent hashing to decide which cache server in a PoP handles a given URL. Prevents the same content from being cached on every server."],["Discord / Riak","Riak (Dynamo-inspired) uses 64 ring partitions per node. Discord used it for message routing across server clusters."],["Redis Cluster","Uses hash slots (0-16383) with CRC16 of the key. Not pure consistent hashing, but the same concept — each node owns a range of hash slots."]]}},{heading:"Consistent Hashing vs Range Partitioning",body:`Consistent hashing is not the only way to partition data. Range partitioning assigns key ranges to nodes (e.g., A-M → node 1, N-Z → node 2). This enables efficient range scans but creates hot spots if your keys aren't uniformly distributed (e.g., all users whose name starts with 'J' go to node 1).

Consistent hashing deliberately destroys key ordering. Keys that are lexicographically close end up on different nodes. This achieves uniform load but makes range scans impossible across multiple nodes — you'd need to query all nodes and merge results.

Choose consistent hashing when: uniform load distribution is the priority and range queries span entire datasets.
Choose range partitioning when: your queries frequently scan ranges of keys and you can accept hot spots or need to ensure key locality.`,callout:{kind:"tip",text:"DynamoDB uses consistent hashing for its partition key — but you add a sort key to enable range queries within a single partition. This is the best of both worlds: hashing for distribution, range indexing within each partition. Understanding this pattern is the foundation of designing DynamoDB schemas."}}]},d={slug:"sql-vs-nosql",title:"SQL vs NoSQL",emoji:"🗃️",category:"Database",tagline:"Choosing the right data store for the job",roadmapKeywords:["sql","nosql","database","postgresql","mongodb","cassandra","dynamo","relational","document","key-value"],related:["database-indexes","acid-transactions","sharding","cap-theorem"],sections:[{heading:"The Relational Model",body:`SQL databases (Relational databases) organise data into tables — rows and columns — with a fixed schema. Relationships between tables are expressed via foreign keys and enforced by the database engine. The query language is SQL (Structured Query Language), which is declarative: you describe what data you want, not how to retrieve it.

The relational model was invented by E.F. Codd at IBM in 1970 and remains dominant for most transactional workloads. Its strengths: strong consistency (ACID guarantees), powerful JOIN operations, referential integrity (foreign keys prevent orphaned data), and a mature ecosystem of tooling, ORMs, and operational expertise.

The relational model's constraints: schema must be defined upfront (migrations required for changes), scaling horizontally (sharding) is harder than scaling vertically, and JOIN-heavy queries don't distribute well across shards.`},{heading:"The 4 NoSQL Families",body:`"NoSQL" is not one thing — it's a category of databases that don't use the relational model. There are four distinct families, each optimised for a different data shape and access pattern.`,table:{cols:["Family","Data model","Examples","Best for"],rows:[["Key-Value","Dictionary: key → opaque blob or structured value","Redis, DynamoDB (KV mode), Memcached, Riak","Sessions, caches, user preferences, rate limiting counters, shopping carts"],["Document","Nested JSON/BSON documents; flexible schema per document","MongoDB, CouchDB, Couchbase, AWS DocumentDB","Product catalogs, user profiles, content management, mobile apps with variable fields"],["Wide-Column","Rows with dynamic columns; rows grouped into column families","Apache Cassandra, HBase, Google Bigtable","Time-series data, event logs, IoT sensor data, write-heavy workloads at massive scale"],["Graph","Nodes (entities) and edges (relationships) with properties","Neo4j, Amazon Neptune, JanusGraph, TigerGraph","Social networks, recommendation engines, fraud detection, knowledge graphs"]]}},{heading:"Head-to-Head Comparison",table:{cols:["Property","SQL (Relational)","NoSQL (general)"],rows:[["Schema","Rigid, defined upfront; ALTER TABLE for changes","Flexible (schema-on-read); documents can have different fields"],["ACID transactions","Strong ACID out of the box","Varies widely: Redis is atomic per command; Cassandra is eventually consistent; MongoDB 4+ supports multi-doc ACID"],["Scaling axis","Vertical (bigger machine) primary; horizontal sharding is complex","Horizontal by design; easy to add shards/nodes"],["JOINs","First-class SQL feature; efficient with indexes","Not natively supported; denormalise data or do application-level joins"],["Query language","SQL — standardised, declarative, portable","Database-specific APIs; no universal standard"],["Consistency","Strong (ACID) by default","Often eventual consistency for availability/partition tolerance (AP in CAP)"],["Write throughput","Limited by single-master (typically)","Very high — leaderless or multi-leader; Cassandra handles millions of writes/sec"],["Data relationships","Normalise into multiple tables; enforce with FK constraints","Embed related data in same document; denormalise for read performance"],["Operational maturity","Very mature (50+ years); excellent tooling, ORMs, DBA expertise","Younger; tooling varies; operational complexity higher"]]}},{heading:"When to Use SQL",bullets:["Your data is relational with clear foreign-key relationships (orders → users → addresses → products).","You need ACID transactions — money transfers, inventory updates, booking systems where partial updates would be catastrophic.","Your schema is stable and well-understood upfront. Relational databases reward careful schema design.","You need complex queries: multi-table JOINs, GROUP BY aggregations, window functions, CTEs. SQL is far more expressive than NoSQL query APIs for ad-hoc analysis.","You're building an OLTP (Online Transaction Processing) system: e-commerce, ERP, banking, CRM, SaaS products. PostgreSQL handles 99% of these perfectly.","Your team has more SQL expertise than NoSQL expertise. Operational familiarity matters enormously."]},{heading:"When to Use NoSQL",bullets:["You need massive write throughput that SQL can't sustain on a single master (10,000+ writes/sec → Cassandra).","Your data is schemaless or evolves rapidly (different fields per user, variable product attributes) → MongoDB/DynamoDB.","You're building a time-series data store for millions of sensor/event writes per second → Cassandra, InfluxDB.","You need a cache or session store with sub-millisecond reads → Redis.","Your queries are primarily single-entity lookups by key, with no complex JOINs needed → DynamoDB.","You're modelling highly connected data (social graph, fraud rings) → Neo4j.","Global multi-region active-active writes with eventual consistency are acceptable → DynamoDB, Cassandra."]},{heading:"Polyglot Persistence",body:`Modern large-scale systems rarely use just one database. They use the right tool for each job — a pattern called polyglot persistence.

Example architecture for a large e-commerce platform:
- PostgreSQL: orders, payments, user accounts (ACID, relational)
- Redis: sessions, shopping cart, rate limiting, real-time inventory counters
- Elasticsearch: full-text product search with faceting and ranking
- Cassandra: clickstream/event logs (append-only, time-series, massive write volume)
- Neo4j: recommendation engine (who bought this also bought…)
- S3: product images, PDFs, raw data lake

Each database handles what it's best at. The trade-off: operational complexity increases significantly. You now need engineers who know all these systems, monitoring for all of them, and backup/recovery procedures for each.`,callout:{kind:"tip",text:"In a system design interview, defaulting to PostgreSQL for your primary data store is a strong choice. Then add Redis for caching/sessions, Elasticsearch for search, and Cassandra/DynamoDB only when you can justify the need for massive horizontal write scale. Don't add complexity without a reason."}},{heading:"NewSQL — The Best of Both?",body:`NewSQL databases attempt to provide the horizontal scalability of NoSQL while maintaining SQL and ACID guarantees. They use consensus algorithms (Paxos, Raft) to synchronise writes across shards while still exposing a standard SQL interface.

Google Spanner (2012) was the first major NewSQL system — globally distributed, externally consistent SQL. CockroachDB and YugabyteDB are open-source NewSQL databases inspired by Spanner.

Trade-offs: higher write latency (synchronous consensus adds round trips), significant operational complexity, and still relatively young compared to PostgreSQL. For most startups and mid-size companies, PostgreSQL with read replicas and connection pooling (PgBouncer) handles 10-100x more load than you think before you need NewSQL.`}]},c={slug:"rate-limiting",title:"Rate Limiting",emoji:"🚦",category:"Networking",tagline:"Protecting your API from being overwhelmed",roadmapKeywords:["rate limit","rate limiting","throttle","token bucket","api gateway","429","leaky bucket"],related:["load-balancing","api-design","caching"],sections:[{heading:"Why Rate Limiting?",body:`An API without rate limits is an invitation to abuse. A single misbehaving client — a broken retry loop, a scraper, a DDoS attack, or a well-meaning developer with a bug — can consume all your server resources and degrade service for every other user.

Rate limiting caps how many requests a client can make within a time window. It protects your system by:
1. Preventing resource exhaustion: database connections, CPU, memory, and bandwidth are finite.
2. Ensuring fair usage: one client can't starve others by monopolising server capacity.
3. Protecting downstream services: if your API calls a payment processor that limits you to 100 req/sec, you need to enforce that limit before hitting it.
4. Enabling tiered pricing: free tier gets 100 req/min; paid tier gets 10,000 req/min. Rate limits enforce the business model.
5. Slowing brute-force attacks: password guessing and credential stuffing attacks depend on making thousands of rapid attempts. Rate limiting makes them economically unviable.`},{heading:"The Token Bucket Algorithm",body:`Token bucket is the most widely used rate limiting algorithm, used by AWS API Gateway, Stripe, GitHub, and most major APIs.

Imagine a bucket with a maximum capacity of B tokens. Tokens are added to the bucket at a fixed rate R tokens/second. Each incoming request consumes one token. If there are no tokens left, the request is rejected (HTTP 429 Too Many Requests).

Properties:
- Burst allowance: if a client sends no requests for 5 seconds, the bucket fills up. It can then burst up to B requests instantly before being throttled. This is crucial for real users who send occasional bursts (opening a page loads 20 resources simultaneously).
- Sustained rate: over a long window, the client cannot exceed R requests/second regardless of burst behaviour.
- Two parameters to tune: R (fill rate = sustained average) and B (bucket capacity = maximum burst size).

Example: R=10 req/sec, B=50 tokens. Client can burst 50 requests instantly, then is limited to 10/sec thereafter.`,diagram:"token-bucket"},{heading:"Rate Limiting Algorithms Compared",table:{cols:["Algorithm","How it works","Allows bursts?","Memory per client","Best for"],rows:[["Token Bucket","Tokens accumulate at rate R up to capacity B. Requests consume tokens.","Yes — up to B requests instantly","O(1) — just current token count + timestamp","Most APIs; AWS API Gateway, Stripe, GitHub"],["Leaky Bucket","Requests enter a queue (bucket); processed at fixed rate. Overflow is dropped.","No — fixed output rate regardless of input","O(queue size) — entire queue stored","Traffic shaping for uniform output; networking QoS"],["Fixed Window Counter","Count requests in current time window (e.g., this minute). Reset at window boundary.","Yes — burst at window start","O(1) — just a counter + window start time","Simple, low-memory; coarse-grained limits"],["Sliding Window Log","Store timestamp of every request. Count how many fall within the last N seconds.","No — precise rolling window","O(requests per window) — all timestamps stored","Precise limits when memory is available"],["Sliding Window Counter","Approximate sliding window: blend current window count + previous window count weighted by position","Slight burst at boundary","O(1) — two counters","Best balance of accuracy and memory; recommended for most production systems"]]},callout:{kind:"warning",text:"Fixed Window Counter has a boundary burst problem: a client can make 100 requests in the last second of window 1 and 100 requests in the first second of window 2 — 200 requests in 2 seconds despite a limit of 100/minute. Sliding Window Counter solves this without the memory cost of storing individual timestamps."}},{heading:"Where to Enforce Rate Limits",bullets:["API Gateway (recommended): Centralize rate limiting at the entry point. AWS API Gateway, Kong, Nginx, Envoy, Traefik all support rate limiting natively. The application servers never see excess traffic.","Middleware layer: If you don't have an API gateway, add rate limiting as middleware in your application server (e.g., express-rate-limit for Node.js, Flask-Limiter for Python). Uses Redis for shared state across multiple app servers.","CDN / Edge: Cloudflare Rate Limiting can enforce limits before requests even reach your infrastructure. Best for DDoS protection — traffic is blocked at the edge, saving origin bandwidth.","Load balancer: HAProxy, Nginx, and AWS ALB support basic request rate limiting. Good for coarse-grained protection.","Service mesh: Envoy and Istio support rate limiting at the sidecar proxy level — useful for service-to-service rate limiting in microservices architectures."],callout:{kind:"tip",text:"Rate limiting at the application layer is fine for a single server. The moment you have multiple instances, you need a shared counter — use Redis. Redis's atomic INCR and EXPIRE commands make it the de-facto standard for distributed rate limiting."}},{heading:"Distributed Rate Limiting",body:`Rate limiting a single server is trivial: store the counter in memory. The challenge appears when you have multiple API server instances behind a load balancer.

Problem: if counter is stored in-process, each of your 10 servers has its own counter. A client can make 100 requests to each server for a total of 1,000 requests — 10x your limit.

Solution 1 — Centralised Redis counter: Every request increments a shared Redis key (e.g., rate:user:12345). Redis's single-threaded atomic INCR ensures no race conditions. EXPIRE sets the window. This adds ~0.5ms per request (one Redis round trip). Works well up to millions of requests/second.

Solution 2 — Sticky load balancing: Route each client to the same server always (IP hash or session affinity). Eliminates distributed counter problem. But creates hot spots and defeats horizontal scaling — not recommended.

Solution 3 — Gossip protocol with approximate counting: Each server tracks its own count and shares with peers periodically. Approximate but avoids central bottleneck. Used at very large scale (Twitter's approach).`},{heading:"HTTP 429 and Client Handling",body:`When a client exceeds a rate limit, your API should return HTTP 429 Too Many Requests with standard headers that tell the client when they can retry.

Standard response headers (de-facto standard, also in RFC 6585):
- X-RateLimit-Limit: the maximum requests allowed in the window
- X-RateLimit-Remaining: how many requests the client has left in the current window
- X-RateLimit-Reset: Unix timestamp when the window resets (or seconds until reset)
- Retry-After: seconds the client should wait before retrying (required by RFC 6585)

Client-side best practices:
1. Exponential backoff with jitter: wait 2^attempt + random(0, 1) seconds before each retry. Prevents all clients retrying simultaneously after a 429.
2. Respect Retry-After: don't retry before the server says it's safe. Respect the header.
3. Circuit breaker: after N consecutive 429s, stop sending requests entirely for a cooldown period. Re-try after the circuit opens.`}]},h={slug:"message-queues",title:"Message Queues",emoji:"📨",category:"Distributed Systems",tagline:"Async communication so services don't wait on each other",roadmapKeywords:["queue","kafka","rabbitmq","pub sub","message","async","event","sqs","broker"],related:["microservices","cap-theorem","replication"],sections:[{heading:"Why Asynchronous Messaging?",body:`In a synchronous architecture, Service A calls Service B and waits for a response before continuing. This works fine when B is fast and always available. But in a distributed system, B can be slow, overloaded, or temporarily down.

If A waits synchronously for B:
- Latency compounds: A's response time = A's time + B's time + network latency
- Failure propagates: if B crashes, A's request fails too
- No buffering: if B is slow, A accumulates open connections until it runs out of resources

Message queues decouple producers (senders) from consumers (receivers) in space, time, and processing rate.

In space: A doesn't need to know B's IP address or even that B exists. A sends to the queue; B reads from the queue.
In time: A can send a message and continue without waiting. B can process the message hours later.
In rate: A can produce 1,000 messages/second; B can consume 100/second. The queue absorbs the burst and B catches up at its own pace — this is called backpressure buffering.`,diagram:"queue-flow"},{heading:"Point-to-Point vs Pub/Sub",table:{cols:["Property","Point-to-Point (Queue)","Publish-Subscribe (Topic)"],rows:[["Model","One producer → One queue → One consumer (at a time)","One producer → One topic → Many consumer groups simultaneously"],["Message delivery","Each message consumed by exactly one consumer","Each message delivered to every subscribed consumer group"],["Use case","Task queues: email sending, image resizing, billing. Each job done once by one worker.","Event broadcasting: order placed → notify inventory + notify email + notify analytics all in parallel"],["Examples","AWS SQS (standard), RabbitMQ queue, ActiveMQ","Apache Kafka, AWS SNS, Google Pub/Sub, RabbitMQ fanout exchange"],["Scaling consumers","Add consumers to the same queue — they compete for messages and share the load","Add consumer groups — each group gets all messages independently"],["Message retention","Message deleted after successful consumption","Messages retained for configurable period (Kafka: days/weeks) regardless of consumption"]]}},{heading:"Delivery Guarantees",body:"One of the most important — and often misunderstood — properties of a message queue is its delivery guarantee. What does the queue promise about whether a message will be delivered?",table:{cols:["Guarantee","Definition","Implementation","Risk","When to use"],rows:[["At-most-once","Message delivered 0 or 1 time — may be lost","Fire and forget; no ack required","Lost messages","Metrics, logs, telemetry — loss is acceptable"],["At-least-once","Message delivered 1 or more times — never lost","Producer retries until ack. Consumer must ack after processing.","Duplicate messages","Most business events — use idempotent consumers"],["Exactly-once","Message delivered exactly 1 time — no loss, no dups","Two-phase commit or idempotent producer + transactional consumer","Very high overhead; distributed transactions","Payments, financial records — but rare; often simulated with idempotency keys"]]},callout:{kind:"warning",text:"Exactly-once is extremely difficult in distributed systems (see CAP theorem). In practice, most systems target at-least-once delivery and make consumers idempotent (processing the same message twice produces the same result as processing it once). Use idempotency keys (unique message IDs) so consumers can detect and discard duplicates."}},{heading:"Kafka vs RabbitMQ vs SQS vs Redis Streams",table:{cols:["System","Model","Retention","Throughput","Ordering","Best for"],rows:[["Apache Kafka","Distributed log / pub-sub","Days to weeks (configurable)","Millions of msg/sec","Ordered within partition","High-throughput event streaming, event sourcing, audit logs, stream processing (Flink, Spark)"],["RabbitMQ","Traditional AMQP broker","Until consumed (or TTL)","Tens of thousands/sec","FIFO within queue","Task queues, request/reply patterns, complex routing (fanout, topic, direct), legacy AMQP integration"],["AWS SQS","Managed queue service","Up to 14 days","Unlimited (managed)","FIFO queue for ordering (extra cost)","Simple task queues on AWS; standard queue for highest throughput, FIFO queue for strict ordering"],["Redis Streams","Append-only log (like mini-Kafka)","Configurable (MAXLEN)","Very high (in-memory)","Ordered by ID","Low-latency pub-sub, real-time notifications, leaderboards with streaming updates"]]}},{heading:"Dead Letter Queues (DLQ)",body:`When a consumer fails to process a message (exception thrown, timeout, invalid data), the queue typically requeues the message and tries again. But what if the message is fundamentally unprocessable — a malformed payload, a dependency that's permanently unavailable, a bug in consumer code?

Without a dead letter queue, the message will be retried forever, blocking other messages and consuming resources endlessly. This is called a poison pill message.

A dead letter queue is a separate queue where messages are moved after a configurable number of failed processing attempts (e.g., after 3 retries). The original queue is unblocked and moves on to healthy messages. Engineers can inspect the DLQ, fix the root cause, and re-queue the messages when ready.

DLQ is supported natively by: AWS SQS, RabbitMQ, Azure Service Bus, Kafka (via a convention — route to a dead-letter topic in your consumer code).

Always configure a DLQ in production. The alternative is silent message loss (if you give up after N retries without a DLQ) or an infinite retry loop poisoning your queue.`},{heading:"Backpressure and Consumer Scaling",body:`Backpressure is the mechanism by which a slow consumer signals to a fast producer to slow down. Without backpressure, an overwhelmed consumer's queue grows indefinitely until memory is exhausted.

In queue-based systems, backpressure is implicit: the queue depth (number of unprocessed messages) is the signal. When queue depth grows, you scale consumers. When it shrinks, you can scale down.

Autoscaling based on queue depth: AWS SQS + Lambda scales automatically based on queue depth. Kubernetes + KEDA can scale deployments based on Kafka consumer lag. This is one of the most powerful patterns for cost-efficient horizontal scaling.

Consumer scaling strategies:
1. Add more consumers to the same queue (competing consumers pattern) — each message processed by one consumer, load spreads automatically
2. Partition-based parallelism (Kafka) — add consumers up to the number of partitions; more partitions = more parallelism possible
3. Vertical scaling — give existing consumers more CPU/memory to process messages faster`,callout:{kind:"tip",text:"Design your consumers to be stateless and idempotent. Stateless means you can add and remove consumer instances at will without coordination. Idempotent means that if the queue accidentally delivers a message twice (at-least-once semantics), processing it twice produces the same result as once."}}]},u={slug:"api-design",title:"API Design",emoji:"🔌",category:"Architecture",tagline:"Designing APIs that developers actually enjoy",roadmapKeywords:["api","rest","graphql","grpc","endpoint","http","idempotent","versioning","pagination"],related:["load-balancing","rate-limiting","microservices"],sections:[{heading:"What Makes a Good API?",body:`An API (Application Programming Interface) is a contract between two systems. A good API is one that developers can understand and use correctly without reading the implementation — it should be intuitive, consistent, and predictable.

The qualities of a great API:
1. Predictability: similar operations look similar. If GET /users/:id returns a user, GET /orders/:id should return an order in the same shape.
2. Consistency: naming conventions, field formats (snake_case vs camelCase), date formats (ISO 8601), pagination shape — all uniform across every endpoint.
3. Discoverability: a developer can infer what endpoints exist from the ones they already know. /users → /users/:id → /users/:id/orders follows naturally.
4. Idempotency: safe to retry. Calling the same endpoint twice produces the same result — crucial for reliability in distributed systems.
5. Good error messages: HTTP status codes + error body that tells the developer exactly what went wrong and how to fix it.`},{heading:"REST — Representational State Transfer",body:`REST is the dominant architectural style for HTTP APIs. It's not a protocol or standard — it's a set of constraints that, when followed, produce scalable, cacheable, and stateless APIs.

Roy Fielding's 6 REST constraints:
1. Client-Server: UI concerns separated from data storage concerns. Client and server evolve independently.
2. Stateless: every request from client to server must contain all information needed to understand the request. No session stored on the server between requests. Session state is client-side.
3. Cacheable: responses must declare themselves cacheable or non-cacheable. Caching eliminates repeated identical requests.
4. Uniform Interface: the defining constraint of REST. Achieved via: resource identification in URIs, manipulation via representations, self-descriptive messages, HATEOAS (hypermedia as the engine of application state).
5. Layered System: client doesn't know if it's talking directly to the server or through intermediaries (LBs, caches, gateways). Intermediaries are transparent.
6. Code on Demand (optional): servers can send executable code (JavaScript) to clients.`},{heading:"HTTP Methods and Idempotency",table:{cols:["Method","Operation","Safe?","Idempotent?","Request body?","Use for"],rows:[["GET","Read","Yes","Yes","No","Fetch resource(s). Never use GET for writes — it's cached and bookmarked."],["POST","Create","No","No","Yes","Create a new resource. NOT idempotent — two POSTs create two resources."],["PUT","Replace","No","Yes","Yes","Replace entire resource. Idempotent — same PUT twice gives same result."],["PATCH","Partial update","No","No*","Yes","Partial update. Can be made idempotent with conditional requests (If-Match ETag)."],["DELETE","Delete","No","Yes","Optional","Delete resource. Idempotent — deleting an already-deleted resource returns 404 but doesn't error."],["HEAD","Read (no body)","Yes","Yes","No","Same as GET but response has no body. Used to check if resource exists or get headers."],["OPTIONS","Capabilities","Yes","Yes","No","CORS preflight. Returns allowed methods for a resource."]]},callout:{kind:"note",text:"Safe means the operation doesn't change server state. Idempotent means calling it N times has the same effect as calling it once. A GET is both safe and idempotent. A DELETE is not safe (it changes state) but is idempotent (deleting twice is same as deleting once). A POST is neither — two identical POSTs create two records."}},{heading:"REST vs GraphQL vs gRPC",table:{cols:["Property","REST","GraphQL","gRPC"],rows:[["Protocol","HTTP/1.1 or HTTP/2","HTTP/1.1 or HTTP/2","HTTP/2 (required)"],["Format","JSON (or XML)","JSON","Protocol Buffers (binary)"],["Schema","OpenAPI/Swagger (optional)","Strongly typed schema (required)","Strongly typed .proto files (required)"],["Fetching","Fixed response shape per endpoint","Client specifies exact fields needed (no over/under-fetching)","Fixed methods defined in .proto"],["Over-fetching","Common — GET /users returns all fields even if you need only name","Eliminated — query { user(id: 1) { name } }","Eliminated — message types are specific"],["Under-fetching","Common — need /users + /users/1/orders (N+1 problem)","Eliminated — single query fetches everything","Eliminated — client defines what it needs"],["Real-time","Webhooks or polling","Subscriptions (WebSocket)","Server streaming, bidirectional streaming"],["Performance","Good","Good (but resolver overhead)","Excellent (binary, multiplexing, streaming)"],["Browser support","Native","Native","Requires grpc-web proxy for browsers"],["Best for","Public APIs, CRUD services, external consumers","Complex data graphs, mobile apps, BFFs (backend for frontend)","Internal service-to-service, low latency, streaming, mobile gRPC"]]}},{heading:"API Versioning Strategies",body:"APIs evolve. Adding new fields to a response is usually backward compatible. Removing fields, changing field types, or changing behaviour breaks existing clients. Versioning lets you make breaking changes while supporting old clients.",table:{cols:["Strategy","Example","Pros","Cons"],rows:[["URL path versioning","GET /api/v2/users","Explicit, easy to route, cacheable, easy to see in browser","Clutters URLs; must maintain N parallel code paths"],["Header versioning","Accept: application/vnd.api+json; version=2","Clean URLs; version is metadata, not path","Not cacheable by CDNs (varies by header); less visible; harder to test in browser"],["Query parameter","GET /api/users?version=2","Easy to test in browser","Can be cached incorrectly if CDN doesn't vary on query params"],["Content negotiation","Accept: application/vnd.company.v2+json","RESTful; self-describing","Complex; not widely adopted; poor tooling support"]]},callout:{kind:"tip",text:"URL path versioning (/v1/, /v2/) is the most pragmatic choice for most teams. It's explicit, CDN-cacheable, and easy to route at the API gateway. Use it by default unless you have a specific reason for header versioning."}},{heading:"Pagination — Handling Large Collections",table:{cols:["Strategy","How it works","Pros","Cons","Best for"],rows:[["Offset pagination","GET /users?offset=200&limit=50 — skip N rows, return M","Simple to implement; allows jumping to arbitrary page","Slow for large offsets (DB scans N rows to skip); page drift (inserts/deletes shift items)","Admin UIs with page numbers; small-to-medium datasets"],["Cursor pagination","GET /users?cursor=eyJ1c2VyX2lkIjoxMjN9&limit=50 — cursor is an opaque pointer to position","Stable (insertions/deletions don't affect pages); efficient for large data","Can't jump to arbitrary page; cursor must be opaque to client","Infinite scroll; feeds; real-time data; large datasets"],["Keyset pagination","GET /users?after_id=12345&limit=50 — explicitly use a sortable key column","Very efficient (uses index); predictable performance","Requires sortable key; can't use arbitrary sort columns","Time-series data; any table with a monotonic ID or timestamp"]]}},{heading:"HTTP Status Codes Cheat Sheet",table:{cols:["Range","Category","Key codes"],rows:[["2xx","Success","200 OK, 201 Created, 204 No Content (DELETE success)"],["3xx","Redirect","301 Moved Permanently, 302 Found (temporary), 304 Not Modified (cached)"],["4xx","Client error","400 Bad Request (validation), 401 Unauthorized (not logged in), 403 Forbidden (logged in but no permission), 404 Not Found, 409 Conflict (duplicate), 422 Unprocessable Entity (semantic validation), 429 Too Many Requests"],["5xx","Server error","500 Internal Server Error, 502 Bad Gateway (upstream failed), 503 Service Unavailable (overloaded/maintenance), 504 Gateway Timeout (upstream too slow)"]]},callout:{kind:"warning",text:"Never return 200 OK with an error body ({ 'status': 'error' }). This breaks HTTP clients, monitoring tools, and CDN caching. Use the correct HTTP status code and put the error detail in the body. Your clients' health checks and alerting depend on it."}}]},m={slug:"replication",title:"Replication",emoji:"📋",category:"Database",tagline:"Keeping copies in sync across machines",roadmapKeywords:["replication","replica","leader","follower","primary","secondary","read replica","sync","async"],related:["cap-theorem","sharding","acid-transactions","consistent-hashing"],sections:[{heading:"Why Replicate?",body:`Replication means keeping a copy of the same data on multiple machines. There are three primary reasons you'd want this:

1. High availability / fault tolerance: if one machine fails, another has the data and can take over. No single point of failure. This is why databases have primary/replica configurations.

2. Read scalability: instead of all read queries hitting one machine, spread them across multiple replicas. Ten replicas → roughly 10x read throughput. Writes still go to one machine (in leader-follower), but reads scale horizontally.

3. Geographic latency reduction: put a read replica physically close to your users. Users in Tokyo read from a Tokyo replica instead of your US-East primary. Reduces round-trip time from 200ms to 5ms.

The fundamental challenge: once you have multiple copies of data, keeping them in sync is hard. Any change on one node must eventually propagate to all other nodes. When and how this happens defines the replication model.`,diagram:"replication-flow"},{heading:"Leader-Follower (Primary-Replica) Replication",body:`The most common replication architecture. One node is designated the leader (primary, master). All write operations must go to the leader. The leader applies the write and propagates the change to all followers (replicas, standby nodes).

Read operations can go to any node — the leader or any follower. Since followers may be slightly behind the leader (replication lag), reading from a follower can return stale data.

Failover: if the leader fails, one of the followers is promoted to leader. This can be manual (DBA intervention) or automatic (using a consensus algorithm to elect a new leader — PostgreSQL + Patroni, MySQL + MHA, MongoDB replica set election).

Advantages: simple mental model; strong consistency for writes (single writer); read scalability via followers.
Disadvantages: leader is the write bottleneck; failover has a brief unavailability window; replication lag can cause read-your-writes anomalies.`},{heading:"Synchronous vs Asynchronous Replication",table:{cols:["Property","Synchronous","Asynchronous"],rows:[["How it works","Leader waits for at least one follower to confirm write before acknowledging client","Leader acknowledges client immediately after local write; propagates to followers in background"],["Write latency","Higher — includes network round trip to follower","Lower — just local disk write"],["Durability","Strong — committed data on ≥2 nodes before ack","Weaker — committed data only on leader at ack time; follower may be behind"],["Data loss on failover","None — at least one follower has the latest data","Possible — follower may not have the last N writes"],["Availability","Lower — if synchronous follower is slow or down, writes block","Higher — leader writes even if all followers are down"],["Network partition","Writes block during partition","Writes continue; diverge with followers"],["Examples","PostgreSQL synchronous_standby_names, MySQL semi-sync, Spanner","PostgreSQL default (async streaming replication), MySQL async binlog, MongoDB default"]]},callout:{kind:"tip",text:"PostgreSQL's recommended production setup: one synchronous replica (prevents data loss on failover) + one or more async replicas (for read scaling without blocking writes). The synchronous replica is your safety net; async replicas are your read scale-out."}},{heading:"Replication Lag and Read Anomalies",body:`Replication lag is the delay between when a write is applied on the leader and when it appears on a follower. In async replication, this can range from milliseconds to seconds (or longer during heavy write load or network issues).

Replication lag causes consistency anomalies for read-from-replica workloads:

Read-your-writes anomaly: user submits a form (write to leader) and immediately refreshes the page (read from follower). The follower hasn't replicated the write yet — the user sees stale data and thinks their write failed. Solution: after a write, route the next read for that user to the leader or wait for the follower to catch up.

Monotonic reads anomaly: user makes two reads from different replicas. Follower A is 100ms behind, Follower B is 1 second behind. User reads from B first, then A — they see older data on the second read. Time appears to go backwards. Solution: route each user's reads to the same replica (sticky reads).

Phantom reads / non-repeatable reads: data changes between two reads within the same "session" because different replicas are at different points. Solution: read from the leader for consistency-sensitive operations.`},{heading:"Multi-Leader Replication",body:`In single-leader replication, all writes go to one machine. Multi-leader (multi-master) replication allows writes to be accepted by any of several leaders simultaneously.

Why multi-leader: multiple data centres — each DC has a local leader that accepts writes. Writes are replicated asynchronously to other DCs' leaders. Users in each DC get low-latency writes.

The write conflict problem: if User A updates their profile in DC-East and simultaneously User B updates the same profile in DC-West, both leaders accept the write. When these writes replicate to each other, they conflict. Who wins?

Conflict resolution strategies:
1. Last-Write-Wins (LWW): use a timestamp; the write with the latest timestamp wins. Simple but loses data — the earlier write is discarded.
2. Merge: application-specific merging (used in Google Docs — CRDT, operational transformation).
3. Custom conflict resolution: expose conflicts to the application for manual resolution (CouchDB).

Multi-leader is complex. Avoid it unless you have a specific cross-DC low-latency write requirement.`},{heading:"Leaderless Replication (Dynamo-style)",body:`Amazon's Dynamo paper (2007) introduced a different model: no leader at all. Any node can accept writes. Reads and writes go to multiple nodes simultaneously, and quorum logic determines consistency.

With N replicas, W write quorum, and R read quorum:
- Write: send to all N replicas; wait for W acknowledgements. If W < N, some replicas may not have the latest data yet.
- Read: send to R replicas; compare responses; return the latest version (using timestamps or vector clocks).
- Strong consistency: R + W > N. If W=2, R=2, N=3 → 2+2=4 > 3. At least one read node always overlaps with the write quorum.

Read repair: when a read detects that some replicas have stale data (their version is older), the system writes the latest version back to those replicas in the background.

Anti-entropy: background process that constantly compares replicas and propagates differences. Ensures eventual convergence even for data that isn't frequently read.

Used by: Apache Cassandra, Amazon DynamoDB, Riak.`,callout:{kind:"note",text:"Leaderless replication gives maximum write availability — you can keep accepting writes even if N-W nodes are down. The trade-off is operational complexity: vector clocks or timestamps for conflict resolution, tuning R/W/N for your consistency vs availability needs, and read repair overhead."}}]},p={slug:"sharding",title:"Sharding",emoji:"🔪",category:"Database",tagline:"Splitting data horizontally across multiple servers",roadmapKeywords:["sharding","partition","horizontal scaling","shard key","database scaling","hot spot"],related:["consistent-hashing","replication","database-indexes","sql-vs-nosql"],sections:[{heading:"Vertical vs Horizontal Scaling",body:`When a single database server can't keep up with load, you have two options:

Vertical scaling (scale up): buy a bigger machine — more CPU cores, more RAM, faster SSD. This is simple (no code changes) but has a hard ceiling. The largest AWS RDS instance as of 2025 has 768 GB RAM and 96 vCPUs. Beyond that limit, you can't scale up further. It's also expensive and creates a single point of failure.

Horizontal scaling (scale out): add more machines. Split your data across them. This is sharding. There's no theoretical ceiling — add 10 more shards when you need them. But it introduces massive complexity in your application and data model.

Sharding is the last resort for database scaling. Before sharding, exhaust these options: read replicas (for read-heavy load), caching (for repeated queries), index optimisation (for slow queries), connection pooling (for connection exhaustion), and query optimisation. Sharding is operationally expensive — do it only when you've exhausted everything else.`,diagram:"sharding-flow"},{heading:"What is Sharding?",body:`Sharding (also called horizontal partitioning) splits a large database table across multiple database instances. Each instance (shard) holds a subset of the rows. The full dataset is distributed across all shards.

A shard key (partition key) determines which shard a given row belongs to. The shard key is a column or set of columns in your data model. Choosing the right shard key is the most critical decision in a sharding architecture — a bad shard key creates hot spots and uneven data distribution that can be worse than not sharding at all.

Example: a social media platform shards its users table by user_id. Users with IDs 1-10M go to shard 1, 10M-20M to shard 2, etc. All data for a given user lives on one shard, so user-specific queries never need to span shards.`},{heading:"Sharding Strategies",table:{cols:["Strategy","How it works","Hot spot risk","Range queries","Resharding difficulty"],rows:[["Range sharding","Split by key ranges: shard 1 = IDs 1-10M, shard 2 = 10M-20M","High — if recent data is popular (e.g., new users are most active), the highest shard gets all traffic","Efficient — all data in a range is on one shard","Hard — range boundaries must be manually re-assigned"],["Hash sharding","hash(shard_key) % N determines shard. Even distribution.","Low — hash function distributes uniformly","Not supported — adjacent keys hash to different shards","Hard — changing N invalidates all hashes (use consistent hashing)"],["Consistent hashing","Keys map to a hash ring; shards own ring segments. See Consistent Hashing.","Low with vnodes","Not supported","Easy — add shard, redistribute only adjacent ring segment"],["Directory sharding","A lookup table maps key ranges to shards. Fully flexible.","Low — lookup table can be rebalanced arbitrarily","Possible if directory maps ranges","Easy — update directory; but directory is a bottleneck and single point of failure"],["Geo sharding","Route by user geography: US users → US shard, EU users → EU shard","Low if traffic distributed globally","Within-region only","Medium — add regions; cross-region queries are expensive"]]}},{heading:"Hot Spots — The #1 Sharding Failure Mode",body:`A hot spot occurs when one shard receives a disproportionate amount of traffic, while other shards sit idle. This defeats the entire purpose of sharding.

Common causes:
1. Popular keys: a celebrity's post on a social network (user_id = Justin Bieber's account). If sharding by user_id, all reads for that post go to one shard.
2. Monotonically increasing keys: if sharding by created_at or an auto-increment ID, all new writes go to the "latest" shard. The newest shard is always the hot shard.
3. Skewed data distribution: if sharding by country and 70% of users are in the US, the US shard gets 70% of traffic.

Solutions:
1. Add entropy to the shard key: instead of user_id, use hash(user_id). Instead of created_at, use hash(user_id + created_at). This distributes writes uniformly at the cost of range query ability.
2. Compound shard key: combine two columns to better distribute load.
3. Split the hot key: for celebrity accounts, use user_id + random_suffix (0-99). Distribute reads across 100 sub-shards. The application queries all 100 and merges results. This is "cell-based" sharding.
4. Caching: a hot key is often hot because it's read frequently. Cache aggressively and only a tiny fraction of reads reach the shard.`,callout:{kind:"warning",text:"Avoid sharding by time (created_at, date) unless your workload accesses historical data uniformly. Append-only workloads (logs, events) with time-based sharding always create a write hot spot on the current shard. Use consistent hashing instead."}},{heading:"Cross-Shard Operations",body:`The hardest problems in a sharded database arise when operations span multiple shards.

Cross-shard queries: SELECT * FROM orders WHERE status = 'pending' — if orders are sharded by user_id, this query must run on every shard and the results merged. This is called a scatter-gather query. At 100 shards, you're making 100 parallel DB queries per request.

Cross-shard joins: impossible to do in the database layer. You must load data from multiple shards in the application and join in code. This is orders of magnitude slower than a SQL JOIN.

Cross-shard transactions: transactions that span multiple shards require a distributed transaction protocol (two-phase commit, 2PC). This is complex, slow, and introduces new failure modes (coordinator failure mid-transaction leaves shards in an inconsistent state).

The golden rule: design your shard key so that 99% of your queries are single-shard queries. If your most frequent query spans all shards, you've chosen the wrong shard key.`},{heading:"Resharding — Adding or Removing Shards",body:`When you eventually outgrow your current shard count and need to add more shards, you must reshard — redistribute data across the new shard topology. This is one of the most dangerous operations in a sharded database.

Naive resharding: change N in hash(key) % N. Every key remaps. 100% of your data needs to move. This requires a full data migration — read from old shards, write to new shards, zero downtime is nearly impossible.

Better approach with consistent hashing: adding a new shard only moves 1/N of keys. The data transfer is proportional, and the system can continue serving traffic during migration with only the keys being moved temporarily in flux.

Online resharding strategies:
1. Read from old, write to new + old: during migration, writes go to both old and new shard locations. Once data is fully copied to new shard, flip reads to new shard and stop writes to old.
2. Version-based routing: maintain a routing version. When resharding, increment version and serve traffic from both topologies, merging results, until migration completes.
3. Vitess (YouTube's sharding layer for MySQL): handles resharding transparently via virtual shards and a routing layer. Moves data in the background with no application changes.`,callout:{kind:"tip",text:"Always shard with future resharding in mind. Use a large virtual shard count (Kafka uses 2048 partitions; Redis Cluster uses 16384 hash slots) — larger than you need now. Remapping virtual shards to physical nodes is cheaper than remapping keys."}}]},g={slug:"cdn",title:"CDN",emoji:"🌐",category:"Networking",tagline:"Delivering content from the edge, close to the user",roadmapKeywords:["cdn","edge","cloudflare","akamai","cache","static assets","ttl","invalidation"],related:["caching","dns","load-balancing"],sections:[{heading:"What is a CDN?",body:`A Content Delivery Network (CDN) is a geographically distributed network of proxy servers (Points of Presence, PoPs) that cache and serve content from locations physically close to end users.

Without a CDN: a user in Sydney requesting your website hosted in US-East waits for a round trip of ~200ms just for the initial TCP handshake, plus another 200ms for TLS, before any content is transferred. Each resource (HTML, CSS, JS, images) adds more round trips. A page with 50 resources can take 4-5 seconds to load from the other side of the world.

With a CDN: that same Sydney user connects to a PoP in Sydney (maybe 5ms away), which has a cached copy of your content. The full page loads in under 500ms. Your US-East server never receives the request.

CDNs serve two functions:
1. Caching static assets (HTML, CSS, JS, images, fonts, video) at the edge — the primary use case.
2. Accelerating dynamic content — the CDN's optimised network backbone routes requests faster than the public internet, even when content can't be cached.`,diagram:"cdn-flow"},{heading:"How CDN Caching Works",body:`When a user requests content, the CDN PoP checks its local cache:

Cache HIT: PoP has a fresh copy → serves immediately from edge. Origin server never involved.
Cache MISS: PoP doesn't have it or it's expired → PoP fetches from origin, caches the response, serves to user. Origin pays the cost only on first request per PoP.

Cache freshness is controlled by HTTP response headers from the origin:
- Cache-Control: max-age=86400 — cache for 24 hours
- Cache-Control: public — cacheable by CDN (and browser)
- Cache-Control: private — browser cache only, NOT the CDN
- Cache-Control: no-store — never cache (login pages, bank transfers)
- Surrogate-Control (Fastly, Akamai): same as Cache-Control but only for CDN layer; not passed to browser

After a cache entry expires (TTL elapsed), the CDN makes a conditional GET to origin (If-None-Match with ETag, or If-Modified-Since). If content unchanged, origin returns 304 Not Modified — no content transferred, CDN extends the cache. Only if content actually changed does origin return a full 200 with the new body.`},{heading:"Cache Invalidation",body:`Cache invalidation is one of the hardest problems in CDNs. Your content is cached across hundreds of PoPs globally. When you deploy a new version of your website, how do you get all PoPs to serve the new content?

Strategy 1 — Content-addressed URLs (best practice): embed a file hash in the filename: app-3f4a9b.js. When the file changes, the hash changes, the URL changes, and the new URL is a cache MISS everywhere → guaranteed fresh content. The old URL expires naturally via TTL. This is what all modern build tools (Vite, webpack) do with code-splitting and asset fingerprinting.

Strategy 2 — CDN Cache Purge API: explicitly send API calls to the CDN to invalidate specific URLs or paths. Cloudflare, Fastly, and AWS CloudFront all provide purge APIs. Fast (seconds) but requires integrating purge calls into your deployment pipeline.

Strategy 3 — Short TTLs: set max-age=60 (1 minute). All caches expire quickly. Simple but means 60% more origin requests — caches don't provide much benefit at 1-minute TTLs.

Strategy 4 — Surrogate keys (cache tags): tag cached content with metadata. When a product is updated, purge all cached responses tagged with that product ID. Fastly and Varnish support this natively.`,callout:{kind:"tip",text:"The correct production setup: use content-addressed URLs with long TTLs (max-age=31536000, 1 year) for versioned assets (JS, CSS, images). Use short TTLs or CDN purge for your HTML files, which reference the new asset URLs. HTML is small and cheap to re-fetch; assets are large and benefit most from long-term caching."}},{heading:"Push vs Pull CDN",table:{cols:["Property","Pull CDN","Push CDN"],rows:[["How it works","CDN fetches content from origin on first cache miss. Content pulled on demand.","You upload content to CDN storage directly. CDN serves from its storage, never fetching from origin."],["Setup","Just point CDN at your origin URL. No pre-upload step.","Must upload all content to CDN before users request it. More complex deployment."],["Best for","Any content with unpredictable access patterns; most web apps.","Large files with predictable access: software downloads, game updates, firmware images, video files."],["Storage cost","CDN only stores what's been requested at least once. Efficient.","You pay for all stored content upfront, even if never accessed."],["Freshness","Easy — set TTL on origin response. Stale content auto-evicted.","Manual — you must re-upload when content changes. Risk of serving stale content."],["Examples","CloudFront (default mode), Cloudflare, Fastly","AWS CloudFront with S3 origin, Akamai NetStorage, BunnyCDN"]]}},{heading:"CDN for Dynamic Content",body:`CDNs aren't just for static files. Modern CDNs can accelerate dynamic content and run code at the edge.

Edge caching for dynamic content: even a 10-second cache of an API response can dramatically reduce origin load for popular endpoints. GET /api/trending/posts → cache for 30 seconds. Each PoP serves thousands of requests from cache; origin gets 1 request per PoP per 30 seconds.

Edge functions / workers: run JavaScript or WebAssembly at the PoP, close to the user. Used for: A/B testing (modify HTML at the edge), personalisation (insert user-specific content), authentication (validate JWT at the edge before proxying), geo-redirects, and bot detection. Examples: Cloudflare Workers, Vercel Edge Functions, Fastly Compute.

Request coalescing (request collapsing): if 1,000 users simultaneously request the same cache-expired URL, the CDN collapses those into a single upstream request. Only one request hits your origin; 999 users wait for that response and receive it simultaneously. Prevents cache stampede against your origin.`},{heading:"CDN Providers",table:{cols:["Provider","Known for","Differentiator"],rows:[["Cloudflare","Security + performance","DDoS protection, DNS, edge workers (Cloudflare Workers), free tier, Argo routing"],["Fastly","Programmable CDN","Varnish-based, instant purge (150ms), Compute@Edge (Wasm), best-in-class cache tags"],["AWS CloudFront","AWS integration","Native S3, ALB, API Gateway origins; Lambda@Edge; tightest AWS IAM integration"],["Akamai","Enterprise, media","Largest PoP count (4,000+ globally); advanced media delivery; DDoS scrubbing"],["Vercel Edge","Frontend frameworks","Seamless Next.js/React deployment; edge functions; automatic static optimisation"],["BunnyCDN","Price performance","Very low cost; pull + push zones; good for storage-heavy workloads"]]}}]},y={slug:"dns",title:"DNS",emoji:"📡",category:"Networking",tagline:"How domain names resolve to IP addresses",roadmapKeywords:["dns","domain","nameserver","ttl","a record","cname","mx","route53","resolve"],related:["cdn","load-balancing","latency"],sections:[{heading:"Why DNS Exists",body:`Computers communicate via IP addresses (e.g., 142.250.80.46). Humans are bad at memorising numbers. DNS (Domain Name System) is the internet's phone book — a globally distributed database that maps human-readable domain names (google.com) to machine-readable IP addresses.

DNS was designed in 1983 by Paul Mockapetris as a replacement for a single HOSTS.TXT file that every computer on the internet downloaded periodically. As the internet grew, a central file became unmanageable. DNS introduced a hierarchical, distributed, and cacheable naming system that scales to billions of domains.

DNS is critical infrastructure: it runs on almost every internet request. A browser loads a page → DNS resolves 20-30 domain names for analytics, fonts, CDNs, APIs. DNS failures or slowness directly impact page load time and availability.`,diagram:"dns-resolution"},{heading:"DNS Resolution — Step by Step",body:`When your browser needs to resolve api.example.com:

1. Browser cache: browsers cache DNS results (Chrome: up to 1 minute). Check here first.
2. OS cache / hosts file: operating system has its own DNS cache. /etc/hosts file on Linux/Mac is checked before any network query.
3. Recursive resolver (DNS resolver): your ISP or configured DNS server (e.g., 8.8.8.8 Google, 1.1.1.1 Cloudflare). This server does the heavy lifting — it queries other DNS servers on your behalf and caches results.
4. Root nameserver: the recursive resolver asks a root server "who handles .com?" There are 13 root server addresses (a.root-servers.net to m.root-servers.net), each operated by different organisations, with thousands of anycast instances worldwide.
5. TLD (Top-Level Domain) nameserver: the root server says "for .com, ask Verisign's nameservers." The recursive resolver asks Verisign "who handles example.com?"
6. Authoritative nameserver: Verisign points to example.com's authoritative nameserver (e.g., Route 53, Cloudflare DNS). This server has the actual DNS records for example.com. It returns the A record: api.example.com → 93.184.216.34.
7. Response cached and returned: recursive resolver caches the result for the record's TTL duration and returns the IP to the browser.

Total time for a cold (uncached) lookup: 50-100ms. Warm (cached): 0ms.`},{heading:"DNS Record Types",table:{cols:["Record Type","Full Name","What it does","Example value"],rows:[["A","Address","Maps domain to IPv4 address","93.184.216.34"],["AAAA","IPv6 Address","Maps domain to IPv6 address","2606:2800:220:1:248:1893:25c8:1946"],["CNAME","Canonical Name","Alias from one domain to another. The target must be an A/AAAA record, not an IP.","api.example.com → app.myhost.com"],["MX","Mail Exchange","Specifies mail server for the domain; has priority (lower = higher priority)","10 mail.example.com, 20 backup-mail.example.com"],["NS","Name Server","Specifies the authoritative DNS servers for the domain","ns1.awsdns-01.com"],["TXT","Text","Arbitrary text; used for domain verification, SPF, DKIM, DMARC","v=spf1 include:_spf.google.com ~all"],["SRV","Service","Specifies host + port for a service. Used by SIP, XMPP, Kubernetes","_http._tcp.example.com → 10 0 80 web.example.com"],["SOA","Start of Authority","Metadata about the zone: primary NS, admin email, serial number, refresh/retry intervals","ns1.example.com. admin.example.com. 2025042901 3600 900 604800 300"],["PTR","Pointer","Reverse DNS: maps IP address back to domain name","34.216.184.93.in-addr.arpa → example.com"],["CAA","Certification Authority Authorization","Restricts which CAs can issue SSL certs for the domain","0 issue 'letsencrypt.org'"]]},callout:{kind:"note",text:"CNAME cannot be used on the zone apex (the root domain, e.g., example.com without a subdomain) — only on subdomains. This is why Cloudflare and Route 53 invented 'ALIAS' or 'ANAME' records: they resolve CNAME-like at the DNS level but return A records, allowing use at the apex. This is how example.com can point to a load balancer DNS name."}},{heading:"TTL — Time to Live",body:`Every DNS record has a TTL (Time to Live) value in seconds. This tells resolvers and caches how long they can store the record before querying again.

High TTL (e.g., 86400 = 24 hours): resolver caches the record for a day. Fewer queries to your authoritative DNS server (lower cost, lower latency for cached lookups). But: if you change the record (e.g., change server IP during an incident), it takes up to TTL seconds for all clients to pick up the change. During a disaster recovery, you might be waiting hours for old IPs to expire globally.

Low TTL (e.g., 60 = 1 minute): DNS changes propagate in ~1 minute globally. But: every resolver re-queries every minute. More DNS query volume, slightly higher cost, and slightly higher lookup latency for uncached requests.

TTL strategy for production:
- Normal operation: use TTL 300-3600 (5 min to 1 hour) for A records. Good balance.
- Before a planned migration: lower TTL to 60-300 a few days before the change. Wait for caches to flush. Make the change. After migration is stable, raise TTL back.
- For static nameserver records (NS): use very high TTL (172800 = 48 hours) — these rarely change.`},{heading:"DNS-Based Load Balancing and Geo-Routing",body:`DNS can be used as a primitive load balancer by returning different IPs for the same domain:

Round-robin DNS: configure multiple A records for the same name: example.com → [1.2.3.4, 5.6.7.8]. Resolvers cycle through them. Not true load balancing (ignores server health and load), but distributes traffic at the DNS level.

Weighted routing (AWS Route 53): assign weights to different records. 80% of queries → production server, 20% → canary server. Useful for canary deployments.

Latency-based routing (AWS Route 53, Google Cloud DNS): return the IP of the endpoint with the lowest network latency from the user's location. A user in Singapore gets the Singapore server's IP automatically.

Geolocation routing: return different IP based on user's country/region. EU users → EU servers (GDPR compliance). US users → US servers.

Health-check-based failover: DNS actively monitors endpoint health. If primary IP is unhealthy, automatically switch DNS responses to secondary IP. Route 53 health checks enable this with ~30 second failover time (faster than TTL expiry with pre-lowered TTL).`},{heading:"DNS Security — DNSSEC, DoH, DoT",body:`DNS was designed in 1983 without security in mind. Original DNS is unencrypted, unauthenticated, and vulnerable to several attacks.

DNS cache poisoning (Kaminsky attack, 2008): attacker floods resolver with forged responses, tricking it into caching a fake IP for a legitimate domain. All users of that resolver are redirected to the attacker's server.

DNSSEC (DNS Security Extensions): adds cryptographic signatures to DNS records. Resolvers verify signatures against a chain of trust rooted at the root zone's signing key. Proves that responses are authentic and unmodified. Doesn't encrypt — just signs.

DNS-over-HTTPS (DoH): encrypts DNS queries inside HTTPS requests. ISP and network observers can't see which domains you're resolving. Used by Firefox (Cloudflare DoH by default), Chrome, and iOS.

DNS-over-TLS (DoT): encrypts DNS queries over TLS on port 853. More transparent than DoH (runs on dedicated port, easier to monitor by network admins). Supported by Android 9+, many enterprise DNS resolvers.`,callout:{kind:"tip",text:"For production systems: use AWS Route 53 or Cloudflare DNS for high-availability authoritative DNS (both offer 100% SLA). Enable health checks with automatic failover. Lower TTL to 60-300 before any planned infrastructure change. Never let TTL be the reason your incident lasts longer."}}]},f={slug:"acid-transactions",title:"ACID Transactions",emoji:"⚗️",category:"Database",tagline:"The four guarantees that make databases trustworthy",roadmapKeywords:["acid","transactions","atomicity","consistency","isolation","durability","sql","rollback"],related:["sql-vs-nosql","replication","cap-theorem","database-indexes"],sections:[{heading:"What is a Transaction?",body:`A database transaction is a sequence of one or more operations (reads and writes) that are treated as a single indivisible unit. Either all operations in the transaction succeed (commit), or none of them take effect (rollback).

The classic example: transferring $100 from Alice's account to Bob's account requires two writes:
1. Deduct $100 from Alice (balance: 1000 → 900)
2. Add $100 to Bob (balance: 500 → 600)

If the server crashes after step 1 but before step 2, Alice has lost $100 and Bob received nothing. The money vanished. Without transactions, this is a real risk. With a transaction, if anything goes wrong, both writes are rolled back atomically — the system returns to a consistent state as if neither write happened.

ACID is a set of four properties that define what a "reliable transaction" means.`},{heading:"A — Atomicity",body:`Atomicity guarantees that a transaction is all-or-nothing. If any part of the transaction fails (due to a crash, constraint violation, network error, or explicit ROLLBACK), the entire transaction is rolled back as if it never happened. Partial updates are never persisted.

Implementation: databases implement atomicity via a Write-Ahead Log (WAL). Before modifying data on disk, the database writes the intended change to the WAL. If the database crashes mid-transaction, on restart it reads the WAL and determines whether to commit (replay the operations) or abort (ignore them). Incomplete transactions in the WAL are rolled back.

Savepoints: SQL supports partial rollbacks within a transaction using SAVEPOINT. You can roll back to a savepoint without aborting the entire transaction. Useful for nested operations where you want to retry a sub-operation.`,callout:{kind:"note",text:"Atomicity is about fault tolerance, not isolation. It says: on failure, no partial state is visible. It doesn't say anything about what concurrent transactions see while the transaction is in progress — that's Isolation."}},{heading:"C — Consistency",body:`Consistency guarantees that a transaction takes the database from one valid state to another valid state. Any data written to the database must satisfy all defined constraints, rules, and cascades.

Consistency is partially the database's job (enforcing CHECK constraints, foreign keys, NOT NULL, UNIQUE) and partially the application's job (ensuring the business logic in a transaction maintains invariants).

Example consistency constraints enforced by the database:
- account.balance >= 0 (CHECK constraint — prevents negative balance)
- orders.user_id references users.id (FOREIGN KEY — prevents orphaned orders)
- users.email UNIQUE (UNIQUE constraint — prevents duplicate accounts)

If a transaction would violate any of these, the database rejects it with an error and rolls back.

Note: Consistency in ACID is subtly different from Consistency in CAP. ACID Consistency = database constraints. CAP Consistency = all nodes see the same data at the same time (linearizability).`},{heading:"I — Isolation",body:`Isolation governs what a transaction sees when other transactions are running concurrently. Without isolation, concurrent transactions would interfere with each other, causing various anomalies.

Isolation is the most nuanced ACID property. It's a spectrum, not a binary. SQL defines four isolation levels, each protecting against a different set of anomalies at different performance costs.

Understanding the anomalies:
- Dirty read: reading uncommitted data from another transaction. If that transaction rolls back, you read data that never "officially" existed.
- Non-repeatable read: reading the same row twice in one transaction and getting different values because another committed transaction modified it between your reads.
- Phantom read: running the same query twice in one transaction and getting different rows because another committed transaction inserted/deleted rows that match your query's WHERE clause.
- Write skew: two transactions both read a shared condition, make a decision based on it, and then write — but together their writes violate the invariant neither write individually would violate (e.g., both approve the "last doctor on call" to take leave simultaneously).`},{heading:"Isolation Levels",table:{cols:["Level","Dirty Read","Non-repeatable Read","Phantom Read","Write Skew","Performance","Implementation"],rows:[["Read Uncommitted","Possible","Possible","Possible","Possible","Fastest","No locks on reads — see everything"],["Read Committed","Prevented","Possible","Possible","Possible","Fast","Only reads committed data. Default in PostgreSQL, Oracle, SQL Server"],["Repeatable Read","Prevented","Prevented","Possible","Possible","Medium","Snapshot of data at transaction start. Default in MySQL InnoDB"],["Serializable","Prevented","Prevented","Prevented","Prevented","Slowest","Transactions appear to execute one-at-a-time. Predicate locks or SSI"]]},callout:{kind:"tip",text:"Most applications are fine with Read Committed (PostgreSQL default). Use Repeatable Read when you need consistent reads of the same rows. Use Serializable only for critical financial operations where write skew would be catastrophic (e.g., double-spending prevention, booking the last seat on a flight). PostgreSQL implements Serializable Snapshot Isolation (SSI) — it's more efficient than traditional locking-based serializability."}},{heading:"D — Durability",body:`Durability guarantees that once a transaction is committed, the data is permanently saved — even if the system crashes immediately after the commit. A committed transaction is never lost.

Implementation: durability is achieved through the WAL (Write-Ahead Log) + fsync.

1. Write-Ahead Log: before the database modifies its data files, it writes the intended change to a sequential log on disk. Sequential writes are much faster than random writes.
2. fsync: after writing to the WAL, the database calls fsync() to force the OS to flush data from its buffer cache to physical disk. Only after fsync returns does the database send a commit acknowledgement to the client.

The fsync story: many databases historically "cheated" by not calling fsync (or accepting OS promises without true flushing). During power failures, this caused data loss even after apparent commits. MongoDB had this issue in early versions. PostgreSQL's fsync call is the reason it's considered highly durable.

Cloud databases: Aurora, Cloud Spanner, CockroachDB write to multiple nodes before acknowledging a commit. Even if a physical disk fails, your commit is on 3+ nodes. Physical durability is handled at the storage layer.`},{heading:"ACID vs BASE",body:`As distributed databases emerged, the strict ACID model was seen as incompatible with the availability and partition tolerance required at internet scale. BASE is an alternative model designed for AP (Available + Partition Tolerant) systems.

BASE stands for: Basically Available, Soft state, Eventually consistent.

- Basically Available: the system remains available even during partial failures (some nodes down), possibly returning stale data.
- Soft state: the system's state may change over time even without new inputs — replication is happening in the background, updating state gradually.
- Eventually consistent: if no new updates are made, all replicas will eventually converge to the same value. Consistency is not guaranteed at any given moment.

ACID ↔ BASE is a spectrum. Modern databases blur the line: MongoDB added multi-document ACID transactions in v4.0. Cassandra has lightweight transactions (Paxos) for specific operations. DynamoDB has transactional APIs for up to 25 items. You can have ACID semantics within a shard of a NoSQL system.`,table:{cols:["Property","ACID","BASE"],rows:[["Consistency","Immediate — all reads see committed data","Eventual — reads may see stale data for a window of time"],["Availability","May sacrifice availability for consistency","Always available, even with partial failures"],["Transactions","Full multi-statement, multi-table transactions","Limited or no multi-document transactions"],["Performance","Write overhead from locking and WAL fsync","Very high throughput — no global locking"],["Use cases","Financial, inventory, bookings, CRM, ERP, any CRUD app","Social feeds, event logs, IoT, time-series, real-time analytics"],["Examples","PostgreSQL, MySQL, Oracle, SQL Server, CockroachDB","Cassandra, DynamoDB, CouchDB (BASE by default)"]]}}]},b={slug:"microservices",title:"Microservices",emoji:"🧩",category:"Architecture",tagline:"Breaking monoliths into independent, deployable pieces",roadmapKeywords:["microservices","monolith","service mesh","api gateway","docker","kubernetes","service discovery"],related:["api-design","message-queues","load-balancing","rate-limiting"],sections:[{heading:"Monolith vs Microservices",body:`A monolithic application has all functionality — user management, orders, payments, inventory, notifications — in a single deployable unit. One codebase, one database, one deployment.

Monoliths are not bad. They're simpler to develop, test, deploy, and debug. For most small and medium products, a well-structured monolith is the right choice. Amazon, Netflix, and Uber all started as monoliths.

Microservices split the monolith into a collection of small, independently deployable services, each owning a specific business domain. Each service:
- Runs as a separate process
- Has its own database (no shared databases)
- Communicates via network calls (HTTP/gRPC/message queues)
- Can be deployed, scaled, and failed independently

The trade-off in one sentence: microservices trade deployment independence and scalability for network complexity, distributed systems challenges, and operational overhead.`,diagram:"microservices-flow"},{heading:"When to Choose Microservices",body:`Don't start with microservices. The distributed systems complexity they introduce is a significant ongoing tax — every engineer must understand network failures, timeouts, distributed tracing, and eventual consistency.

Consider microservices when:
1. Different parts of your system have wildly different scaling requirements (e.g., image processing needs 100x more CPU than user authentication — it makes sense to scale them independently).
2. Different teams own different domains and need independent deployment cadences — a team shouldn't need to coordinate with 5 other teams to deploy their service.
3. You need to use different technology stacks for different services (ML model serving in Python, user API in Go, legacy integration in Java).
4. Your monolith has become so large that builds take 30+ minutes and a small change requires deploying and testing the entire application.
5. You have the operational maturity: container orchestration (Kubernetes), distributed tracing, service mesh, CI/CD pipelines, and on-call culture.

Signs you're NOT ready: if you can't deploy your monolith confidently and reliably, microservices will make things worse. Fix your deployment pipeline first.`,callout:{kind:"warning",text:"Microservices are the solution to an organisational problem as much as a technical one. If Conway's Law applies (systems mirror the communication structure of the organisations that build them), then one team building a microservices architecture produces a distributed monolith — tightly coupled services that must be deployed together. Team structure should align with service boundaries."}},{heading:"API Gateway",body:`In a microservices architecture, clients should not call individual services directly. An API Gateway is the single entry point for all external clients.

Responsibilities of an API gateway:
1. Routing: proxy requests to the appropriate service based on path, method, headers.
2. Authentication & authorisation: validate JWTs, API keys, OAuth tokens before requests reach services. Services trust that requests arriving from the gateway are authenticated.
3. Rate limiting: centralised rate limiting per client before traffic reaches services.
4. SSL termination: decrypt HTTPS at the gateway; internal traffic can be plain HTTP.
5. Request transformation: translate between external API formats and internal service APIs. Add/remove headers, reshape request bodies.
6. Response aggregation (BFF pattern): combine responses from multiple services into a single client response, reducing round trips.
7. Load balancing: distribute traffic across service instances.
8. Observability: centralised logging, metrics, and distributed trace header injection.

Examples: AWS API Gateway, Kong, Envoy, Nginx, Traefik, Netflix Zuul.`},{heading:"Service Communication Patterns",table:{cols:["Pattern","Protocol","Style","When to use","Trade-off"],rows:[["Synchronous HTTP/REST","HTTP/1.1 or HTTP/2 + JSON","Request-response","Standard CRUD operations, when caller needs an immediate response","Temporal coupling — caller blocks; service B failure directly affects service A"],["Synchronous gRPC","HTTP/2 + Protocol Buffers","Request-response","High-throughput internal APIs, bidirectional streaming, polyglot services","Browser support requires grpc-web proxy; learning curve for .proto files"],["Async messaging (queue)","Message broker (Kafka, SQS)","Fire-and-forget","Background jobs, notifications, events where caller doesn't need response now","Eventual consistency; harder to debug; DLQ management required"],["Event-driven (pub/sub)","Message broker (Kafka, SNS)","Broadcast events","One service event needs to trigger many services simultaneously","Harder to trace event chains; ordering guarantees complex across topics"],["Service mesh (sidecar)","mTLS via Envoy/Istio","Transparent proxy","Zero-trust networking, circuit breaking, retries, distributed tracing at infra level","Significant complexity; Istio learning curve; performance overhead of sidecar"]]}},{heading:"Service Discovery",body:`In a microservices environment, services scale up and down dynamically. IP addresses of service instances change constantly. How does Service A know where to send requests to Service B?

Client-side service discovery: Service A queries a service registry (e.g., Consul, etcd, Eureka) to get the current list of Service B's IP addresses and load-balances across them itself. Gives the client full control; requires client-side load balancing logic.

Server-side service discovery: Service A sends requests to a load balancer or API gateway. The load balancer queries the service registry and routes to a healthy Service B instance. The client is unaware of the registry. Simpler clients; load balancer is the single point of control (and potential failure).

DNS-based service discovery: each service has a DNS name (e.g., payment-service.svc.cluster.local in Kubernetes). DNS returns the IP of a healthy pod. Kubernetes uses this model — kube-dns (CoreDNS) maintains records for every service. Changes in pod IPs are automatically reflected in DNS.

Kubernetes service discovery: Kubernetes Services are the primitive. A Service object provides a stable IP (ClusterIP) and DNS name. kube-proxy routes ClusterIP traffic to a healthy pod. Developers only need to know the service name.`},{heading:"Data Isolation — Database per Service",body:`The most important rule in microservices: each service must own its data exclusively. No two services share a database. If two services share a database, they are not truly independent — a schema migration in one breaks the other, and you have a distributed monolith.

Consequences of database-per-service:
1. No cross-service SQL JOINs. Data that would naturally be joined in a monolith must be composed in the application layer or via eventual consistency.
2. Eventual consistency: if Order Service creates an order and sends a message to Inventory Service to decrement stock, there's a window where the order exists but inventory hasn't decremented. Design for this.
3. Distributed transactions: operations that span multiple services (e.g., place order + deduct inventory + charge payment) cannot use a database transaction. Use the Saga pattern instead.

Saga pattern: a sequence of local transactions, each publishing an event/message to trigger the next step. On failure, compensating transactions undo previous steps. Two flavours: choreography (each service reacts to events and publishes new events) and orchestration (a central saga orchestrator coordinates all steps).`,callout:{kind:"tip",text:"In a system design interview, always mention the data isolation principle when discussing microservices. Then bring up eventual consistency and the Saga pattern for cross-service operations. This signals deep understanding beyond just 'services talk to each other via APIs'."}},{heading:"Distributed Tracing and Observability",body:`In a monolith, debugging a slow request is straightforward: check the logs and profiler for that request. In microservices, a single user request might traverse 10 services. Which service caused the 500ms latency spike?

Distributed tracing addresses this by generating a unique trace ID for each incoming request and propagating it through every service call as an HTTP header (e.g., X-Trace-ID, W3C Trace-Context). Each service records its own span (start time, duration, service name, operation name) tagged with the trace ID.

A tracing backend (Jaeger, Zipkin, AWS X-Ray, Datadog APM) collects all spans with the same trace ID and reconstructs the full request timeline as a flame graph. You can instantly see: Service A took 5ms, Service B (which A called) took 450ms (the culprit), and B's database query took 430ms.

The three pillars of microservices observability:
- Logs: structured JSON logs with trace ID in every line
- Metrics: RED metrics per service — Request rate, Error rate, Duration (latency)
- Traces: distributed traces correlating all spans for a single request

Without all three, debugging production issues in a microservices environment is guesswork.`}]},t=[a,s,o,i,r,n,l,d,c,h,u,m,p,g,y,f,b],w=new Map(t.map(e=>[e.slug,e])),v=Array.from(new Set(t.map(e=>e.category)));export{t as C,w as a,v as b};
